// Code generated by Scalar SDK Generator. DO NOT EDIT.
//! Report-writing smoke harness: drives every synthesizable operation once
//! and writes a per-operation JSON report.
//!
//! Run it with `cargo run --example smoke-test`. The generator runs this
//! example against a mock server and reads the JSON report written to the
//! path named by `SCALAR_SMOKE_REPORT`, honoring an optional
//! `SCALAR_SMOKE_FILTER` of comma-separated operation/path substrings.
#![allow(unused)]

use profound::*;

#[derive(serde::Serialize)]
struct SmokeResult {
    operation: String,
    method: String,
    path: String,
    /// Which of an operation's two calls this is; empty when it contributed only one.
    #[serde(skip_serializing_if = "String::is_empty")]
    label: String,
    status: String,
    #[serde(rename = "durationMs")]
    duration_ms: i64,
    #[serde(skip_serializing_if = "String::is_empty")]
    error: String,
}

#[derive(serde::Serialize)]
struct SmokeReport {
    total: usize,
    failed: usize,
    results: Vec<SmokeResult>,
}

/// Whether an operation/path is selected by the optional comma-separated
/// `SCALAR_SMOKE_FILTER`; with no filter every operation runs.
fn selected(filter: &Option<Vec<String>>, operation: &str, path: &str) -> bool {
    match filter {
        None => true,
        Some(needles) => needles
            .iter()
            .any(|needle| operation.contains(needle.as_str()) || path.contains(needle.as_str())),
    }
}

#[tokio::main]
async fn main() {
    let report_path = std::env::var("SCALAR_SMOKE_REPORT").ok();
    let filter = std::env::var("SCALAR_SMOKE_FILTER").ok().map(|raw| {
        raw.split(',')
            .map(|needle| needle.trim().to_string())
            .filter(|needle| !needle.is_empty())
            .collect::<Vec<_>>()
    });
    let client = ProfoundClient::from_env().expect("client builds from environment");
    let mut results: Vec<SmokeResult> = Vec::new();
    if selected(&filter, "regions", "/v1/org/regions") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client.organizations().regions().send().await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "regions".to_string(),
            method: "GET".to_string(),
            path: "/v1/org/regions".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "regions", "/v1/org/regions") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .organizations()
                .regions()
                .organization_ids(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()])
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "regions".to_string(),
            method: "GET".to_string(),
            path: "/v1/org/regions".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "models", "/v1/org/models") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client.organizations().models().send().await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "models".to_string(),
            method: "GET".to_string(),
            path: "/v1/org/models".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "domains", "/v1/org/domains") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client.organizations().domains().send().await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "domains".to_string(),
            method: "GET".to_string(),
            path: "/v1/org/domains".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "domains", "/v1/org/domains") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .organizations()
                .domains()
                .organization_ids(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()])
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "domains".to_string(),
            method: "GET".to_string(),
            path: "/v1/org/domains".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "listAssets", "/v1/org/assets") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client.organizations().list_assets().send().await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "listAssets".to_string(),
            method: "GET".to_string(),
            path: "/v1/org/assets".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "listAssets", "/v1/org/assets") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .organizations()
                .list_assets()
                .organization_ids(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()])
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "listAssets".to_string(),
            method: "GET".to_string(),
            path: "/v1/org/assets".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "getPersonas", "/v1/org/personas") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client.organizations().get_personas().send().await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "getPersonas".to_string(),
            method: "GET".to_string(),
            path: "/v1/org/personas".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "getPersonas", "/v1/org/personas") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .organizations()
                .get_personas()
                .organization_ids(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()])
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "getPersonas".to_string(),
            method: "GET".to_string(),
            path: "/v1/org/personas".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "list", "/v1/org") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client.organizations().list().send().await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "list".to_string(),
            method: "GET".to_string(),
            path: "/v1/org".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "list", "/v1/org/categories") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client.organizations().categories().list().send().await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "list".to_string(),
            method: "GET".to_string(),
            path: "/v1/org/categories".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "list", "/v1/org/categories") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .organizations()
                .categories()
                .list()
                .organization_ids(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()])
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "list".to_string(),
            method: "GET".to_string(),
            path: "/v1/org/categories".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "topics", "/v1/org/categories/{category_id}/topics") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .organizations()
                .categories()
                .topics("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "topics".to_string(),
            method: "GET".to_string(),
            path: "/v1/org/categories/{category_id}/topics".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "tags", "/v1/org/categories/{category_id}/tags") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .organizations()
                .categories()
                .tags("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "tags".to_string(),
            method: "GET".to_string(),
            path: "/v1/org/categories/{category_id}/tags".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "prompts", "/v1/org/categories/{category_id}/prompts") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .organizations()
                .categories()
                .prompts("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "prompts".to_string(),
            method: "GET".to_string(),
            path: "/v1/org/categories/{category_id}/prompts".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "prompts", "/v1/org/categories/{category_id}/prompts") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .organizations()
                .categories()
                .prompts("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .limit(10000)
                .cursor("example")
                .order_by(PromptOrderBy::CreatedAt)
                .order_dir(OrderByDirection::Asc)
                .analysis_type(vec![AnalysisType::Visibility])
                .prompt_type(vec![PromptType::Visibility])
                .status(vec!["active".to_string()])
                .topic_id(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()])
                .tag_id(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()])
                .region_id(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()])
                .platform_id(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()])
                .persona_id(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()])
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "prompts".to_string(),
            method: "GET".to_string(),
            path: "/v1/org/categories/{category_id}/prompts".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "assets", "/v1/org/categories/{category_id}/assets") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .organizations()
                .categories()
                .assets("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "assets".to_string(),
            method: "GET".to_string(),
            path: "/v1/org/categories/{category_id}/assets".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(
        &filter,
        "getCategoryPersonas",
        "/v1/org/categories/{category_id}/personas",
    ) {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .organizations()
                .categories()
                .get_category_personas("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "getCategoryPersonas".to_string(),
            method: "GET".to_string(),
            path: "/v1/org/categories/{category_id}/personas".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "createPrompts", "/v1/org/categories/{category_id}/prompts") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .organizations()
                .categories()
                .create_prompts(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    CreatePromptsBody {
                        prompts: vec![CreatePromptInput {
                            id: None,
                            prompt: "x".to_string(),
                            topic: IdOrName { id: None, name: None },
                            language: "".to_string(),
                            tags: None,
                            regions: vec![IdOrName { id: None, name: None }],
                            platforms: vec![IdOrName { id: None, name: None }],
                            personas: None,
                            analysis_types: None,
                            prompt_type: None,
                            asset: None,
                        }],
                        dry_run: None,
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createPrompts".to_string(),
            method: "POST".to_string(),
            path: "/v1/org/categories/{category_id}/prompts".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "createPrompts", "/v1/org/categories/{category_id}/prompts") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .organizations()
                .categories()
                .create_prompts(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    CreatePromptsBody {
                        prompts: vec![CreatePromptInput {
                            id: None,
                            prompt: "x".to_string(),
                            topic: IdOrName { id: None, name: None },
                            language: "".to_string(),
                            tags: None,
                            regions: vec![IdOrName { id: None, name: None }],
                            platforms: vec![IdOrName { id: None, name: None }],
                            personas: None,
                            analysis_types: None,
                            prompt_type: None,
                            asset: None,
                        }],
                        dry_run: Some(false),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createPrompts".to_string(),
            method: "POST".to_string(),
            path: "/v1/org/categories/{category_id}/prompts".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "updatePrompts", "/v1/org/categories/{category_id}/prompts") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .organizations()
                .categories()
                .update_prompts(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    UpdatePromptsBody {
                        prompts: vec![UpdatePromptInput {
                            id: "".to_string(),
                            prompt: None,
                            topic: None,
                            language: None,
                            tags: None,
                            regions: None,
                            platforms: None,
                            personas: None,
                            analysis_types: None,
                            prompt_type: None,
                            asset: None,
                        }],
                        dry_run: None,
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "updatePrompts".to_string(),
            method: "PATCH".to_string(),
            path: "/v1/org/categories/{category_id}/prompts".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "updatePrompts", "/v1/org/categories/{category_id}/prompts") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .organizations()
                .categories()
                .update_prompts(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    UpdatePromptsBody {
                        prompts: vec![UpdatePromptInput {
                            id: "".to_string(),
                            prompt: None,
                            topic: None,
                            language: None,
                            tags: None,
                            regions: None,
                            platforms: None,
                            personas: None,
                            analysis_types: None,
                            prompt_type: None,
                            asset: None,
                        }],
                        dry_run: Some(false),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "updatePrompts".to_string(),
            method: "PATCH".to_string(),
            path: "/v1/org/categories/{category_id}/prompts".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(
        &filter,
        "updatePromptStatus",
        "/v1/org/categories/{category_id}/prompts/status",
    ) {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .organizations()
                .categories()
                .update_prompt_status(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    UpdatePromptStatusBody {
                        prompt_ids: vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()],
                        status: UpdatePromptStatusBodyStatus::Active,
                        dry_run: None,
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "updatePromptStatus".to_string(),
            method: "PATCH".to_string(),
            path: "/v1/org/categories/{category_id}/prompts/status".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(
        &filter,
        "updatePromptStatus",
        "/v1/org/categories/{category_id}/prompts/status",
    ) {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .organizations()
                .categories()
                .update_prompt_status(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    UpdatePromptStatusBody {
                        prompt_ids: vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()],
                        status: UpdatePromptStatusBodyStatus::Active,
                        dry_run: Some(false),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "updatePromptStatus".to_string(),
            method: "PATCH".to_string(),
            path: "/v1/org/categories/{category_id}/prompts/status".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "retrieveRegions", "/v1/org/categories/{category_id}/regions") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .organizations()
                .categories()
                .retrieve_regions("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "retrieveRegions".to_string(),
            method: "GET".to_string(),
            path: "/v1/org/categories/{category_id}/regions".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(
        &filter,
        "getCitationCategories",
        "/v1/org/categories/{category_id}/citation-categories",
    ) {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .organizations()
                .categories()
                .get_citation_categories("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "getCitationCategories".to_string(),
            method: "GET".to_string(),
            path: "/v1/org/categories/{category_id}/citation-categories".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "answers", "/v1/prompts/answers") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .prompts()
                .answers(AnswersQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    pagination: None,
                    filters: None,
                    include: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "answers".to_string(),
            method: "POST".to_string(),
            path: "/v1/prompts/answers".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "answers", "/v1/prompts/answers") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .prompts()
                .answers(AnswersQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    pagination: Some(Pagination {
                        limit: None,
                        offset: None,
                    }),
                    filters: None,
                    include: Some(EnabledFieldsResponse {
                        run_id: None,
                        created_at: None,
                        prompt: None,
                        prompt_id: None,
                        mentions: None,
                        analysis_types: None,
                        prompt_type: None,
                        response: None,
                        citations: None,
                        citation_details: None,
                        web_search_results: None,
                        search_triggered: None,
                        themes: None,
                        sentiment_themes: None,
                        topic: None,
                        topic_id: None,
                        region: None,
                        model: None,
                        model_id: None,
                        asset: None,
                        asset_id: None,
                        tags: None,
                        search_queries: None,
                        persona: None,
                    }),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "answers".to_string(),
            method: "POST".to_string(),
            path: "/v1/prompts/answers".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "answersV2", "/v2/prompts/answers") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .prompts()
                .answers_v2(AnswersV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    include: None,
                    filter: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "answersV2".to_string(),
            method: "POST".to_string(),
            path: "/v2/prompts/answers".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "answersV2", "/v2/prompts/answers") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .prompts()
                .answers_v2(AnswersV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    include: Some(vec![AnswersV2QueryInclude::RunId]),
                    filter: None,
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "answersV2".to_string(),
            method: "POST".to_string(),
            path: "/v2/prompts/answers".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamAnswersV2", "/v2/prompts/answers/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .prompts()
                .stream_answers_v2(AnswersV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    include: None,
                    filter: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamAnswersV2".to_string(),
            method: "POST".to_string(),
            path: "/v2/prompts/answers/stream".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamAnswersV2", "/v2/prompts/answers/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .prompts()
                .stream_answers_v2(AnswersV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    include: Some(vec![AnswersV2QueryInclude::RunId]),
                    filter: None,
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamAnswersV2".to_string(),
            method: "POST".to_string(),
            path: "/v2/prompts/answers/stream".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "citations", "/v1/reports/citations") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .citations(CitationsQuery {
                    date_interval: None,
                    dimensions: None,
                    metrics: vec![CitationsQueryMetric::Count],
                    order_by: None,
                    pagination: None,
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    filters: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "citations".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/citations".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "citations", "/v1/reports/citations") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .citations(CitationsQuery {
                    date_interval: Some(CitationsQueryDateInterval::Day),
                    dimensions: Some(vec![]),
                    metrics: vec![CitationsQueryMetric::Count],
                    order_by: Some(std::collections::HashMap::from([])),
                    pagination: Some(Pagination {
                        limit: None,
                        offset: None,
                    }),
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    filters: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "citations".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/citations".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "visibility", "/v1/reports/visibility") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .visibility(VisibilityQuery {
                    date_interval: None,
                    dimensions: None,
                    metrics: vec![VisibilityQueryMetric::ShareOfVoice],
                    order_by: None,
                    pagination: None,
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    filters: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "visibility".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/visibility".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "visibility", "/v1/reports/visibility") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .visibility(VisibilityQuery {
                    date_interval: Some(VisibilityQueryDateInterval::Day),
                    dimensions: Some(vec![]),
                    metrics: vec![VisibilityQueryMetric::ShareOfVoice],
                    order_by: Some(std::collections::HashMap::from([])),
                    pagination: Some(Pagination {
                        limit: None,
                        offset: None,
                    }),
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    filters: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "visibility".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/visibility".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "sentiment", "/v1/reports/sentiment") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .sentiment(SentimentQuery {
                    date_interval: None,
                    dimensions: None,
                    metrics: vec![SentimentQueryMetric::Positive],
                    order_by: None,
                    pagination: None,
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    filters: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "sentiment".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/sentiment".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "sentiment", "/v1/reports/sentiment") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .sentiment(SentimentQuery {
                    date_interval: Some(SentimentQueryDateInterval::Day),
                    dimensions: Some(vec![]),
                    metrics: vec![SentimentQueryMetric::Positive],
                    order_by: Some(std::collections::HashMap::from([])),
                    pagination: Some(Pagination {
                        limit: None,
                        offset: None,
                    }),
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    filters: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "sentiment".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/sentiment".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "sentimentV2", "/v1/reports/sentiment-v2") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .sentiment_v2(SentimentV2ReportQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    asset_name: "".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    comparison_start_date: None,
                    comparison_end_date: None,
                    date_bucket: None,
                    dimensions: None,
                    metrics: vec![SentimentV2ReportQueryMetric::Sentiment],
                    filters: None,
                    order_by: None,
                    pagination: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "sentimentV2".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/sentiment-v2".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "sentimentV2", "/v1/reports/sentiment-v2") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .sentiment_v2(SentimentV2ReportQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    asset_name: "".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    comparison_start_date: Some(chrono::Utc::now().fixed_offset()),
                    comparison_end_date: Some(chrono::Utc::now().fixed_offset()),
                    date_bucket: Some(SentimentV2ReportQueryDateBucket::Day),
                    dimensions: Some(vec![SentimentV2ReportQueryDimension::Date]),
                    metrics: vec![SentimentV2ReportQueryMetric::Sentiment],
                    filters: None,
                    order_by: Some(std::collections::HashMap::from([(
                        "occurrence".to_string(),
                        SentimentV2ReportQueryOrderBy::Desc,
                    )])),
                    pagination: Some(Pagination {
                        limit: None,
                        offset: None,
                    }),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "sentimentV2".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/sentiment-v2".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "getReferralsReport", "/v1/reports/referrals") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .get_referrals_report(ReferralsQuery {
                    date_interval: None,
                    dimensions: None,
                    metrics: vec![ReferralsQueryMetric::Visits],
                    order_by: None,
                    pagination: None,
                    domain: "".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: None,
                    organization_id: None,
                    metric_filters: None,
                    filters: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "getReferralsReport".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/referrals".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "getReferralsReport", "/v1/reports/referrals") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .get_referrals_report(ReferralsQuery {
                    date_interval: Some(ReferralsQueryDateInterval::Day),
                    dimensions: Some(vec![]),
                    metrics: vec![ReferralsQueryMetric::Visits],
                    order_by: Some(std::collections::HashMap::from([])),
                    pagination: Some(Pagination {
                        limit: None,
                        offset: None,
                    }),
                    domain: "".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: Some(chrono::Utc::now().fixed_offset()),
                    organization_id: Some("7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()),
                    metric_filters: None,
                    filters: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "getReferralsReport".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/referrals".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "getBotsReport", "/v1/reports/bots") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .get_bots_report(BotsReportQuery {
                    date_interval: None,
                    dimensions: None,
                    metrics: vec![BotsReportQueryMetric::Count],
                    order_by: None,
                    pagination: None,
                    domain: "".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: None,
                    organization_id: None,
                    metric_filters: None,
                    filters: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "getBotsReport".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/bots".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "getBotsReport", "/v1/reports/bots") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .get_bots_report(BotsReportQuery {
                    date_interval: Some(BotsReportQueryDateInterval::Day),
                    dimensions: Some(vec![]),
                    metrics: vec![BotsReportQueryMetric::Count],
                    order_by: Some(std::collections::HashMap::from([])),
                    pagination: Some(Pagination {
                        limit: None,
                        offset: None,
                    }),
                    domain: "".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: Some(chrono::Utc::now().fixed_offset()),
                    organization_id: Some("7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()),
                    metric_filters: None,
                    filters: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "getBotsReport".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/bots".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "queryFanouts", "/v1/reports/query-fanouts") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .query_fanouts(QueryFanoutsQuery {
                    date_interval: None,
                    dimensions: None,
                    metrics: vec![QueryFanoutsQueryMetric::FanoutsPerExecution],
                    order_by: None,
                    pagination: None,
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    filters: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "queryFanouts".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/query-fanouts".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "queryFanouts", "/v1/reports/query-fanouts") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .query_fanouts(QueryFanoutsQuery {
                    date_interval: Some(QueryFanoutsQueryDateInterval::Day),
                    dimensions: Some(vec![]),
                    metrics: vec![QueryFanoutsQueryMetric::FanoutsPerExecution],
                    order_by: Some(std::collections::HashMap::from([])),
                    pagination: Some(Pagination {
                        limit: None,
                        offset: None,
                    }),
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    filters: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "queryFanouts".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/query-fanouts".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamCitations", "/v1/reports/citations/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .stream_citations(StreamCitationsQuery {
                    date_interval: None,
                    dimensions: None,
                    metrics: vec![StreamCitationsQueryMetric::Count],
                    order_by: None,
                    pagination: None,
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    filters: None,
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamCitations".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/citations/stream".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamCitations", "/v1/reports/citations/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .stream_citations(StreamCitationsQuery {
                    date_interval: Some(StreamCitationsQueryDateInterval::Day),
                    dimensions: Some(vec![]),
                    metrics: vec![StreamCitationsQueryMetric::Count],
                    order_by: Some(std::collections::HashMap::from([])),
                    pagination: Some(Pagination {
                        limit: None,
                        offset: None,
                    }),
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    filters: None,
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamCitations".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/citations/stream".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamVisibility", "/v1/reports/visibility/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .stream_visibility(StreamVisibilityQuery {
                    date_interval: None,
                    dimensions: None,
                    metrics: vec![StreamVisibilityQueryMetric::ShareOfVoice],
                    order_by: None,
                    pagination: None,
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    filters: None,
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamVisibility".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/visibility/stream".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamVisibility", "/v1/reports/visibility/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .stream_visibility(StreamVisibilityQuery {
                    date_interval: Some(StreamVisibilityQueryDateInterval::Day),
                    dimensions: Some(vec![]),
                    metrics: vec![StreamVisibilityQueryMetric::ShareOfVoice],
                    order_by: Some(std::collections::HashMap::from([])),
                    pagination: Some(Pagination {
                        limit: None,
                        offset: None,
                    }),
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    filters: None,
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamVisibility".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/visibility/stream".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamSentiment", "/v1/reports/sentiment/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .stream_sentiment(StreamSentimentQuery {
                    date_interval: None,
                    dimensions: None,
                    metrics: vec![StreamSentimentQueryMetric::Positive],
                    order_by: None,
                    pagination: None,
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    filters: None,
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamSentiment".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/sentiment/stream".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamSentiment", "/v1/reports/sentiment/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .stream_sentiment(StreamSentimentQuery {
                    date_interval: Some(StreamSentimentQueryDateInterval::Day),
                    dimensions: Some(vec![]),
                    metrics: vec![StreamSentimentQueryMetric::Positive],
                    order_by: Some(std::collections::HashMap::from([])),
                    pagination: Some(Pagination {
                        limit: None,
                        offset: None,
                    }),
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    filters: None,
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamSentiment".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/sentiment/stream".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamCitationsV2", "/v2/reports/citations/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .stream_citations_v2(CitationsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    entity: None,
                    group_by: None,
                    metrics: None,
                    interval: None,
                    scope: None,
                    filter: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamCitationsV2".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/citations/stream".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamCitationsV2", "/v2/reports/citations/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .stream_citations_v2(CitationsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    entity: Some(CitationsV2QueryEntity::Domain),
                    group_by: Some(vec![CitationsV2QueryGroupBy::Page]),
                    metrics: Some(vec![CitationsV2QueryMetric::Count]),
                    interval: Some(CitationsV2QueryInterval::Day),
                    scope: Some(CitationsV2QueryScope::All),
                    filter: None,
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamCitationsV2".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/citations/stream".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamVisibilityV2", "/v2/reports/visibility/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .stream_visibility_v2(VisibilityV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: None,
                    metrics: None,
                    interval: None,
                    scope: None,
                    assets: None,
                    filter: None,
                    sort: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamVisibilityV2".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/visibility/stream".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamVisibilityV2", "/v2/reports/visibility/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .stream_visibility_v2(VisibilityV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: Some(vec![VisibilityV2QueryGroupBy::Date]),
                    metrics: Some(vec![VisibilityV2QueryMetric::VisibilityScore]),
                    interval: Some(VisibilityV2QueryInterval::Day),
                    scope: Some(VisibilityV2QueryScope::Owned),
                    assets: None,
                    filter: None,
                    sort: Some(AppRoutesV2AnswerEngineInsightsReportsVisibilitySortSpec { field: None }),
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamVisibilityV2".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/visibility/stream".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamSentimentV2", "/v2/reports/sentiment/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .stream_sentiment_v2(SentimentV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    asset: "".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: None,
                    comparison_end_date: None,
                    group_by: None,
                    metrics: None,
                    interval: None,
                    filter: None,
                    sort: None,
                    include_cited_websites: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamSentimentV2".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/sentiment/stream".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamSentimentV2", "/v2/reports/sentiment/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .stream_sentiment_v2(SentimentV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    asset: "".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: Some("".to_string()),
                    comparison_end_date: Some("".to_string()),
                    group_by: Some(vec![SentimentV2QueryGroupBy::Date]),
                    metrics: Some(vec![SentimentV2QueryMetric::PositiveSentiment]),
                    interval: Some(SentimentV2QueryInterval::Day),
                    filter: None,
                    sort: Some(AppRoutesV2AnswerEngineInsightsReportsSentimentSortSpec { field: None, dir: None }),
                    include_cited_websites: Some(false),
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamSentimentV2".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/sentiment/stream".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamQueryFanouts", "/v2/reports/query-fanouts/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .stream_query_fanouts(QueryFanoutsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: None,
                    metrics: None,
                    interval: None,
                    filter: None,
                    sort: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamQueryFanouts".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/query-fanouts/stream".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamQueryFanouts", "/v2/reports/query-fanouts/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .stream_query_fanouts(QueryFanoutsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: Some(vec![QueryFanoutsV2QueryGroupBy::Date]),
                    metrics: Some(vec![QueryFanoutsV2QueryMetric::FanoutsPerExecution]),
                    interval: Some(QueryFanoutsV2QueryInterval::Day),
                    filter: None,
                    sort: Some(AppRoutesV2AnswerEngineInsightsReportsQueryFanoutsSortSpec {
                        field: "".to_string(),
                        dir: None,
                    }),
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamQueryFanouts".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/query-fanouts/stream".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "getReferralsReportV2", "/v2/reports/referrals") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .get_referrals_report_v2(ReferralsQueryV2 {
                    date_interval: None,
                    dimensions: None,
                    metrics: vec![ReferralsQueryV2Metric::Visits],
                    order_by: None,
                    pagination: None,
                    domain: "".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: None,
                    organization_id: None,
                    timezone: None,
                    view_id: None,
                    metric_filters: None,
                    filters: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "getReferralsReportV2".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/referrals".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "getReferralsReportV2", "/v2/reports/referrals") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .get_referrals_report_v2(ReferralsQueryV2 {
                    date_interval: Some(ReferralsQueryV2DateInterval::Day),
                    dimensions: Some(vec![]),
                    metrics: vec![ReferralsQueryV2Metric::Visits],
                    order_by: Some(std::collections::HashMap::from([])),
                    pagination: Some(Pagination {
                        limit: None,
                        offset: None,
                    }),
                    domain: "".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: Some(chrono::Utc::now().fixed_offset()),
                    organization_id: Some("7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()),
                    timezone: Some("UTC".to_string()),
                    view_id: Some("7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()),
                    metric_filters: None,
                    filters: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "getReferralsReportV2".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/referrals".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "queryVisibility", "/v2/reports/visibility") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .query_visibility(VisibilityV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: None,
                    metrics: None,
                    interval: None,
                    scope: None,
                    assets: None,
                    filter: None,
                    sort: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "queryVisibility".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/visibility".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "queryVisibility", "/v2/reports/visibility") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .query_visibility(VisibilityV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: Some(vec![VisibilityV2QueryGroupBy::Date]),
                    metrics: Some(vec![VisibilityV2QueryMetric::VisibilityScore]),
                    interval: Some(VisibilityV2QueryInterval::Day),
                    scope: Some(VisibilityV2QueryScope::Owned),
                    assets: None,
                    filter: None,
                    sort: Some(AppRoutesV2AnswerEngineInsightsReportsVisibilitySortSpec { field: None }),
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "queryVisibility".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/visibility".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "queryCitations", "/v2/reports/citations") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .query_citations(CitationsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    entity: None,
                    group_by: None,
                    metrics: None,
                    interval: None,
                    scope: None,
                    filter: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "queryCitations".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/citations".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "queryCitations", "/v2/reports/citations") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .query_citations(CitationsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    entity: Some(CitationsV2QueryEntity::Domain),
                    group_by: Some(vec![CitationsV2QueryGroupBy::Page]),
                    metrics: Some(vec![CitationsV2QueryMetric::Count]),
                    interval: Some(CitationsV2QueryInterval::Day),
                    scope: Some(CitationsV2QueryScope::All),
                    filter: None,
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "queryCitations".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/citations".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "querySentiment", "/v2/reports/sentiment") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .query_sentiment(SentimentV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    asset: "".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: None,
                    comparison_end_date: None,
                    group_by: None,
                    metrics: None,
                    interval: None,
                    filter: None,
                    sort: None,
                    include_cited_websites: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "querySentiment".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/sentiment".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "querySentiment", "/v2/reports/sentiment") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .query_sentiment(SentimentV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    asset: "".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: Some("".to_string()),
                    comparison_end_date: Some("".to_string()),
                    group_by: Some(vec![SentimentV2QueryGroupBy::Date]),
                    metrics: Some(vec![SentimentV2QueryMetric::PositiveSentiment]),
                    interval: Some(SentimentV2QueryInterval::Day),
                    filter: None,
                    sort: Some(AppRoutesV2AnswerEngineInsightsReportsSentimentSortSpec { field: None, dir: None }),
                    include_cited_websites: Some(false),
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "querySentiment".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/sentiment".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "queryQueryFanouts", "/v2/reports/query-fanouts") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .query_query_fanouts(QueryFanoutsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: None,
                    metrics: None,
                    interval: None,
                    filter: None,
                    sort: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "queryQueryFanouts".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/query-fanouts".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "queryQueryFanouts", "/v2/reports/query-fanouts") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .query_query_fanouts(QueryFanoutsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: Some(vec![QueryFanoutsV2QueryGroupBy::Date]),
                    metrics: Some(vec![QueryFanoutsV2QueryMetric::FanoutsPerExecution]),
                    interval: Some(QueryFanoutsV2QueryInterval::Day),
                    filter: None,
                    sort: Some(AppRoutesV2AnswerEngineInsightsReportsQueryFanoutsSortSpec {
                        field: "".to_string(),
                        dir: None,
                    }),
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "queryQueryFanouts".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/query-fanouts".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "query", "/v1/reports/web-search-results") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .web_search_results()
                .query(WebSearchResultsQuery {
                    date_interval: None,
                    dimensions: None,
                    metrics: vec![WebSearchResultsQueryMetric::Count],
                    order_by: None,
                    pagination: None,
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    filters: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "query".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/web-search-results".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "query", "/v1/reports/web-search-results") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .web_search_results()
                .query(WebSearchResultsQuery {
                    date_interval: Some(WebSearchResultsQueryDateInterval::Day),
                    dimensions: Some(vec![]),
                    metrics: vec![WebSearchResultsQueryMetric::Count],
                    order_by: Some(std::collections::HashMap::from([])),
                    pagination: Some(Pagination {
                        limit: None,
                        offset: None,
                    }),
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    filters: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "query".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/web-search-results".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "stream", "/v1/reports/web-search-results/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .web_search_results()
                .stream(StreamWebSearchResultsQuery {
                    date_interval: None,
                    dimensions: None,
                    metrics: vec![StreamWebSearchResultsQueryMetric::Count],
                    order_by: None,
                    pagination: None,
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    filters: None,
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "stream".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/web-search-results/stream".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "stream", "/v1/reports/web-search-results/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .web_search_results()
                .stream(StreamWebSearchResultsQuery {
                    date_interval: Some(StreamWebSearchResultsQueryDateInterval::Day),
                    dimensions: Some(vec![]),
                    metrics: vec![StreamWebSearchResultsQueryMetric::Count],
                    order_by: Some(std::collections::HashMap::from([])),
                    pagination: Some(Pagination {
                        limit: None,
                        offset: None,
                    }),
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: chrono::Utc::now().fixed_offset(),
                    end_date: chrono::Utc::now().fixed_offset(),
                    filters: None,
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "stream".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/web-search-results/stream".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "brands", "/v2/reports/shopping/brands") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .shopping()
                .brands(ShoppingBrandsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: None,
                    metrics: None,
                    interval: None,
                    scope: None,
                    assets: None,
                    filter: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "brands".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/shopping/brands".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "brands", "/v2/reports/shopping/brands") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .shopping()
                .brands(ShoppingBrandsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: Some(vec![ShoppingBrandsV2QueryGroupBy::Date]),
                    metrics: Some(vec![ShoppingBrandsV2QueryMetric::VisibilityScore]),
                    interval: Some(ShoppingBrandsV2QueryInterval::Day),
                    scope: Some(ShoppingBrandsV2QueryScope::Owned),
                    assets: None,
                    filter: None,
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "brands".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/shopping/brands".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamBrands", "/v2/reports/shopping/brands/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .shopping()
                .stream_brands(ShoppingBrandsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: None,
                    metrics: None,
                    interval: None,
                    scope: None,
                    assets: None,
                    filter: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamBrands".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/shopping/brands/stream".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamBrands", "/v2/reports/shopping/brands/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .shopping()
                .stream_brands(ShoppingBrandsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: Some(vec![ShoppingBrandsV2QueryGroupBy::Date]),
                    metrics: Some(vec![ShoppingBrandsV2QueryMetric::VisibilityScore]),
                    interval: Some(ShoppingBrandsV2QueryInterval::Day),
                    scope: Some(ShoppingBrandsV2QueryScope::Owned),
                    assets: None,
                    filter: None,
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamBrands".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/shopping/brands/stream".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "products", "/v2/reports/shopping/products") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .shopping()
                .products(ShoppingProductsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: None,
                    metrics: None,
                    interval: None,
                    include_merchants: None,
                    target_product: None,
                    competitor_limit: None,
                    filter: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "products".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/shopping/products".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "products", "/v2/reports/shopping/products") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .shopping()
                .products(ShoppingProductsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: Some(vec![ShoppingProductsV2QueryGroupBy::Date]),
                    metrics: Some(vec![ShoppingProductsV2QueryMetric::VisibilityScore]),
                    interval: Some(ShoppingProductsV2QueryInterval::Day),
                    include_merchants: Some(false),
                    target_product: Some("x".to_string()),
                    competitor_limit: Some(5),
                    filter: None,
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "products".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/shopping/products".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamProducts", "/v2/reports/shopping/products/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .shopping()
                .stream_products(ShoppingProductsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: None,
                    metrics: None,
                    interval: None,
                    include_merchants: None,
                    target_product: None,
                    competitor_limit: None,
                    filter: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamProducts".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/shopping/products/stream".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamProducts", "/v2/reports/shopping/products/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .shopping()
                .stream_products(ShoppingProductsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: Some(vec![ShoppingProductsV2QueryGroupBy::Date]),
                    metrics: Some(vec![ShoppingProductsV2QueryMetric::VisibilityScore]),
                    interval: Some(ShoppingProductsV2QueryInterval::Day),
                    include_merchants: Some(false),
                    target_product: Some("x".to_string()),
                    competitor_limit: Some(5),
                    filter: None,
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamProducts".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/shopping/products/stream".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "merchants", "/v2/reports/shopping/merchants") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .shopping()
                .merchants(ShoppingMerchantsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: None,
                    metrics: None,
                    interval: None,
                    filter: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "merchants".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/shopping/merchants".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "merchants", "/v2/reports/shopping/merchants") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .shopping()
                .merchants(ShoppingMerchantsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: Some(vec![ShoppingMerchantsV2QueryGroupBy::Date]),
                    metrics: Some(vec![ShoppingMerchantsV2QueryMetric::MerchantShare]),
                    interval: Some(ShoppingMerchantsV2QueryInterval::Day),
                    filter: None,
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "merchants".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/shopping/merchants".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamMerchants", "/v2/reports/shopping/merchants/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .shopping()
                .stream_merchants(ShoppingMerchantsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: None,
                    metrics: None,
                    interval: None,
                    filter: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamMerchants".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/shopping/merchants/stream".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamMerchants", "/v2/reports/shopping/merchants/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .shopping()
                .stream_merchants(ShoppingMerchantsV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: Some(vec![ShoppingMerchantsV2QueryGroupBy::Date]),
                    metrics: Some(vec![ShoppingMerchantsV2QueryMetric::MerchantShare]),
                    interval: Some(ShoppingMerchantsV2QueryInterval::Day),
                    filter: None,
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamMerchants".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/shopping/merchants/stream".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "triggerRate", "/v2/reports/shopping/trigger-rate") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .shopping()
                .trigger_rate(ShoppingTriggerRateV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: None,
                    metrics: None,
                    interval: None,
                    filter: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "triggerRate".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/shopping/trigger-rate".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "triggerRate", "/v2/reports/shopping/trigger-rate") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .shopping()
                .trigger_rate(ShoppingTriggerRateV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: Some(vec![ShoppingTriggerRateV2QueryGroupBy::Date]),
                    metrics: Some(vec![ShoppingTriggerRateV2QueryMetric::TotalRuns]),
                    interval: Some(ShoppingTriggerRateV2QueryInterval::Day),
                    filter: None,
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "triggerRate".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/shopping/trigger-rate".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamTriggerRate", "/v2/reports/shopping/trigger-rate/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .shopping()
                .stream_trigger_rate(ShoppingTriggerRateV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: None,
                    metrics: None,
                    interval: None,
                    filter: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamTriggerRate".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/shopping/trigger-rate/stream".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamTriggerRate", "/v2/reports/shopping/trigger-rate/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .shopping()
                .stream_trigger_rate(ShoppingTriggerRateV2Query {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: Some(vec![ShoppingTriggerRateV2QueryGroupBy::Date]),
                    metrics: Some(vec![ShoppingTriggerRateV2QueryMetric::TotalRuns]),
                    interval: Some(ShoppingTriggerRateV2QueryInterval::Day),
                    filter: None,
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamTriggerRate".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/shopping/trigger-rate/stream".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "createOverview", "/v1/reports/accuracy/overview") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_overview(AccuracyOverviewQuery {
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: None,
                    comparison_end_date: None,
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    topic_ids: None,
                    exclude_topic_ids: None,
                    tag_ids: None,
                    tag_filter_type: None,
                    include_no_tag: None,
                    region_ids: None,
                    platform_ids: None,
                    persona_ids: None,
                    include_no_persona: None,
                    prompt_ids: None,
                    citation_categories: None,
                    date_bucket: None,
                    group_by: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createOverview".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/overview".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "createOverview", "/v1/reports/accuracy/overview") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_overview(AccuracyOverviewQuery {
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: Some("".to_string()),
                    comparison_end_date: Some("".to_string()),
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    topic_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    exclude_topic_ids: Some(false),
                    tag_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    tag_filter_type: Some(AccuracyOverviewQueryTagFilterType::Any),
                    include_no_tag: Some(false),
                    region_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    platform_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    persona_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    include_no_persona: Some(false),
                    prompt_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    citation_categories: Some(vec!["".to_string()]),
                    date_bucket: Some("".to_string()),
                    group_by: Some(AccuracyOverviewQueryGroupBy::Period),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createOverview".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/overview".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "createBreakdown", "/v1/reports/accuracy/breakdown") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_breakdown(AccuracyBreakdownQuery {
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: None,
                    comparison_end_date: None,
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    topic_ids: None,
                    exclude_topic_ids: None,
                    tag_ids: None,
                    tag_filter_type: None,
                    include_no_tag: None,
                    region_ids: None,
                    platform_ids: None,
                    persona_ids: None,
                    include_no_persona: None,
                    prompt_ids: None,
                    citation_categories: None,
                    breakdown_by: None,
                    group_by: None,
                    date_bucket: None,
                    limit: None,
                    offset: None,
                    search_query: None,
                    sort_by: None,
                    sort_order: None,
                    pagination: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createBreakdown".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/breakdown".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "createBreakdown", "/v1/reports/accuracy/breakdown") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_breakdown(AccuracyBreakdownQuery {
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: Some("".to_string()),
                    comparison_end_date: Some("".to_string()),
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    topic_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    exclude_topic_ids: Some(false),
                    tag_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    tag_filter_type: Some(AccuracyBreakdownQueryTagFilterType::Any),
                    include_no_tag: Some(false),
                    region_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    platform_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    persona_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    include_no_persona: Some(false),
                    prompt_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    citation_categories: Some(vec!["".to_string()]),
                    breakdown_by: Some(AccuracyBreakdownQueryBreakdownBy::Citation),
                    group_by: Some(vec![AccuracyBreakdownQueryGroupBy::Platform]),
                    date_bucket: Some("".to_string()),
                    limit: Some(10),
                    offset: Some(0),
                    search_query: Some("".to_string()),
                    sort_by: Some(AccuracyBreakdownQuerySortBy::CitationShare),
                    sort_order: Some(AccuracyBreakdownQuerySortOrder::Desc),
                    pagination: Some(AccuracyPagination {
                        dimension: None,
                        metric: None,
                        mode: None,
                        direction: None,
                    }),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createBreakdown".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/breakdown".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(
        &filter,
        "createCitationAnalysis",
        "/v1/reports/accuracy/citation-analysis",
    ) {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_citation_analysis(AccuracyCitationAnalysisQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    clean_href: "".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createCitationAnalysis".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/citation-analysis".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "createTopicIds", "/v1/reports/accuracy/topic-ids") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_topic_ids(AccuracyTopicIdsQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createTopicIds".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/topic-ids".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(
        &filter,
        "createInaccurateThemes",
        "/v1/reports/accuracy/inaccurate-themes",
    ) {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_inaccurate_themes(InaccurateThemesQuery {
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: None,
                    comparison_end_date: None,
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    topic_ids: None,
                    exclude_topic_ids: None,
                    tag_ids: None,
                    tag_filter_type: None,
                    include_no_tag: None,
                    region_ids: None,
                    platform_ids: None,
                    persona_ids: None,
                    include_no_persona: None,
                    prompt_ids: None,
                    citation_categories: None,
                    limit: None,
                    offset: None,
                    sort_by: None,
                    sort_order: None,
                    search_query: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createInaccurateThemes".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/inaccurate-themes".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(
        &filter,
        "createInaccurateThemes",
        "/v1/reports/accuracy/inaccurate-themes",
    ) {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_inaccurate_themes(InaccurateThemesQuery {
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: Some("".to_string()),
                    comparison_end_date: Some("".to_string()),
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    topic_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    exclude_topic_ids: Some(false),
                    tag_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    tag_filter_type: Some(InaccurateThemesQueryTagFilterType::Any),
                    include_no_tag: Some(false),
                    region_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    platform_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    persona_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    include_no_persona: Some(false),
                    prompt_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    citation_categories: Some(vec!["".to_string()]),
                    limit: Some(10),
                    offset: Some(0),
                    sort_by: Some("response_share".to_string()),
                    sort_order: Some(InaccurateThemesQuerySortOrder::Desc),
                    search_query: Some("".to_string()),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createInaccurateThemes".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/inaccurate-themes".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(
        &filter,
        "createInaccurateClusters",
        "/v1/reports/accuracy/inaccurate-clusters",
    ) {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_inaccurate_clusters(InaccurateClustersQuery {
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: None,
                    comparison_end_date: None,
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    topic_ids: None,
                    exclude_topic_ids: None,
                    tag_ids: None,
                    tag_filter_type: None,
                    include_no_tag: None,
                    region_ids: None,
                    platform_ids: None,
                    persona_ids: None,
                    include_no_persona: None,
                    prompt_ids: None,
                    citation_categories: None,
                    theme_id: None,
                    limit: None,
                    offset: None,
                    search_query: None,
                    include_models: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createInaccurateClusters".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/inaccurate-clusters".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(
        &filter,
        "createInaccurateClusters",
        "/v1/reports/accuracy/inaccurate-clusters",
    ) {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_inaccurate_clusters(InaccurateClustersQuery {
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: Some("".to_string()),
                    comparison_end_date: Some("".to_string()),
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    topic_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    exclude_topic_ids: Some(false),
                    tag_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    tag_filter_type: Some(InaccurateClustersQueryTagFilterType::Any),
                    include_no_tag: Some(false),
                    region_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    platform_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    persona_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    include_no_persona: Some(false),
                    prompt_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    citation_categories: Some(vec!["".to_string()]),
                    theme_id: Some("7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()),
                    limit: Some(5000),
                    offset: Some(0),
                    search_query: Some("".to_string()),
                    include_models: Some(false),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createInaccurateClusters".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/inaccurate-clusters".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(
        &filter,
        "createInaccuracyDrivers",
        "/v1/reports/accuracy/inaccuracy-drivers",
    ) {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_inaccuracy_drivers(InaccuracyDriversQuery {
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: None,
                    comparison_end_date: None,
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    topic_ids: None,
                    exclude_topic_ids: None,
                    tag_ids: None,
                    tag_filter_type: None,
                    include_no_tag: None,
                    region_ids: None,
                    platform_ids: None,
                    persona_ids: None,
                    include_no_persona: None,
                    prompt_ids: None,
                    citation_categories: None,
                    limit: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createInaccuracyDrivers".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/inaccuracy-drivers".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(
        &filter,
        "createInaccuracyDrivers",
        "/v1/reports/accuracy/inaccuracy-drivers",
    ) {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_inaccuracy_drivers(InaccuracyDriversQuery {
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: Some("".to_string()),
                    comparison_end_date: Some("".to_string()),
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    topic_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    exclude_topic_ids: Some(false),
                    tag_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    tag_filter_type: Some(InaccuracyDriversQueryTagFilterType::Any),
                    include_no_tag: Some(false),
                    region_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    platform_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    persona_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    include_no_persona: Some(false),
                    prompt_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    citation_categories: Some(vec!["".to_string()]),
                    limit: Some(5),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createInaccuracyDrivers".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/inaccuracy-drivers".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(
        &filter,
        "createTopInaccurateClaims",
        "/v1/reports/accuracy/top-inaccurate-claims",
    ) {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_top_inaccurate_claims(TopInaccurateClaimsQuery {
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: None,
                    comparison_end_date: None,
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    topic_ids: None,
                    exclude_topic_ids: None,
                    tag_ids: None,
                    tag_filter_type: None,
                    include_no_tag: None,
                    region_ids: None,
                    platform_ids: None,
                    persona_ids: None,
                    include_no_persona: None,
                    prompt_ids: None,
                    citation_categories: None,
                    limit: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createTopInaccurateClaims".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/top-inaccurate-claims".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(
        &filter,
        "createTopInaccurateClaims",
        "/v1/reports/accuracy/top-inaccurate-claims",
    ) {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_top_inaccurate_claims(TopInaccurateClaimsQuery {
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: Some("".to_string()),
                    comparison_end_date: Some("".to_string()),
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    topic_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    exclude_topic_ids: Some(false),
                    tag_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    tag_filter_type: Some(TopInaccurateClaimsQueryTagFilterType::Any),
                    include_no_tag: Some(false),
                    region_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    platform_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    persona_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    include_no_persona: Some(false),
                    prompt_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    citation_categories: Some(vec!["".to_string()]),
                    limit: Some(5),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createTopInaccurateClaims".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/top-inaccurate-claims".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "createClaimBreakdown", "/v1/reports/accuracy/claim-breakdown") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_claim_breakdown(ClaimBreakdownQuery {
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: None,
                    comparison_end_date: None,
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    topic_ids: None,
                    exclude_topic_ids: None,
                    tag_ids: None,
                    tag_filter_type: None,
                    include_no_tag: None,
                    region_ids: None,
                    platform_ids: None,
                    persona_ids: None,
                    include_no_persona: None,
                    prompt_ids: None,
                    citation_categories: None,
                    cluster_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createClaimBreakdown".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/claim-breakdown".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "createClaimBreakdown", "/v1/reports/accuracy/claim-breakdown") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_claim_breakdown(ClaimBreakdownQuery {
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: Some("".to_string()),
                    comparison_end_date: Some("".to_string()),
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    topic_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    exclude_topic_ids: Some(false),
                    tag_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    tag_filter_type: Some(ClaimBreakdownQueryTagFilterType::Any),
                    include_no_tag: Some(false),
                    region_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    platform_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    persona_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    include_no_persona: Some(false),
                    prompt_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    citation_categories: Some(vec!["".to_string()]),
                    cluster_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createClaimBreakdown".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/claim-breakdown".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "createClaimCitations", "/v1/reports/accuracy/claim-citations") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_claim_citations(ClaimCitationsQuery {
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: None,
                    comparison_end_date: None,
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    topic_ids: None,
                    exclude_topic_ids: None,
                    tag_ids: None,
                    tag_filter_type: None,
                    include_no_tag: None,
                    region_ids: None,
                    platform_ids: None,
                    persona_ids: None,
                    include_no_persona: None,
                    prompt_ids: None,
                    citation_categories: None,
                    cluster_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    limit: None,
                    offset: None,
                    search_query: None,
                    sort_order: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createClaimCitations".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/claim-citations".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "createClaimCitations", "/v1/reports/accuracy/claim-citations") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_claim_citations(ClaimCitationsQuery {
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: Some("".to_string()),
                    comparison_end_date: Some("".to_string()),
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    topic_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    exclude_topic_ids: Some(false),
                    tag_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    tag_filter_type: Some(ClaimCitationsQueryTagFilterType::Any),
                    include_no_tag: Some(false),
                    region_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    platform_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    persona_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    include_no_persona: Some(false),
                    prompt_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    citation_categories: Some(vec!["".to_string()]),
                    cluster_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    limit: Some(10),
                    offset: Some(0),
                    search_query: Some("".to_string()),
                    sort_order: Some(ClaimCitationsQuerySortOrder::Desc),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createClaimCitations".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/claim-citations".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(
        &filter,
        "createClusterExampleRuns",
        "/v1/reports/accuracy/cluster-example-runs",
    ) {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_cluster_example_runs(ClusterExampleRunsQuery {
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: None,
                    comparison_end_date: None,
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    topic_ids: None,
                    exclude_topic_ids: None,
                    tag_ids: None,
                    tag_filter_type: None,
                    include_no_tag: None,
                    region_ids: None,
                    platform_ids: None,
                    persona_ids: None,
                    include_no_persona: None,
                    prompt_ids: None,
                    citation_categories: None,
                    cluster_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    limit: None,
                    offset: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createClusterExampleRuns".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/cluster-example-runs".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(
        &filter,
        "createClusterExampleRuns",
        "/v1/reports/accuracy/cluster-example-runs",
    ) {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_cluster_example_runs(ClusterExampleRunsQuery {
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    comparison_start_date: Some("".to_string()),
                    comparison_end_date: Some("".to_string()),
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    topic_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    exclude_topic_ids: Some(false),
                    tag_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    tag_filter_type: Some(ClusterExampleRunsQueryTagFilterType::Any),
                    include_no_tag: Some(false),
                    region_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    platform_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    persona_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    include_no_persona: Some(false),
                    prompt_ids: Some(vec!["7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string()]),
                    citation_categories: Some(vec!["".to_string()]),
                    cluster_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    limit: Some(20),
                    offset: Some(0),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createClusterExampleRuns".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/cluster-example-runs".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(
        &filter,
        "createClusterVerificationPairs",
        "/v1/reports/accuracy/cluster-verification-pairs",
    ) {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_cluster_verification_pairs(ClusterVerificationPairsQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    cluster_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createClusterVerificationPairs".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/cluster-verification-pairs".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(
        &filter,
        "createFactcheckSetupStatus",
        "/v1/reports/accuracy/factcheck-setup-status",
    ) {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .accuracy()
                .create_factcheck_setup_status(FactCheckSetupStatusQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "createFactcheckSetupStatus".to_string(),
            method: "POST".to_string(),
            path: "/v1/reports/accuracy/factcheck-setup-status".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "queryScores", "/v2/reports/factcheck") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .factcheck()
                .query_scores(FactcheckScoresQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: None,
                    filter: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "queryScores".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/factcheck".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "queryScores", "/v2/reports/factcheck") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .factcheck()
                .query_scores(FactcheckScoresQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: Some(vec![FactcheckScoresQueryGroupBy::Date]),
                    filter: None,
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "queryScores".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/factcheck".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamScores", "/v2/reports/factcheck/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .factcheck()
                .stream_scores(FactcheckScoresQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: None,
                    filter: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamScores".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/factcheck/stream".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamScores", "/v2/reports/factcheck/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .factcheck()
                .stream_scores(FactcheckScoresQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: Some(vec![FactcheckScoresQueryGroupBy::Date]),
                    filter: None,
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamScores".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/factcheck/stream".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "queryClaims", "/v2/reports/factcheck/claims") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .factcheck()
                .claims()
                .query_claims(FactcheckClaimsQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: None,
                    filter: None,
                    include: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "queryClaims".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/factcheck/claims".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "queryClaims", "/v2/reports/factcheck/claims") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .factcheck()
                .claims()
                .query_claims(FactcheckClaimsQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: Some(vec![FactcheckClaimsQueryGroupBy::Model]),
                    filter: None,
                    include: Some(vec![FactcheckClaimsQueryInclude::Theme]),
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "queryClaims".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/factcheck/claims".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamClaims", "/v2/reports/factcheck/claims/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .factcheck()
                .claims()
                .stream_claims(FactcheckClaimsQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: None,
                    filter: None,
                    include: None,
                    limit: None,
                    max_results: None,
                    cursor: None,
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamClaims".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/factcheck/claims/stream".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "streamClaims", "/v2/reports/factcheck/claims/stream") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            use futures::StreamExt;
            let mut events = client
                .reports()
                .factcheck()
                .claims()
                .stream_claims(FactcheckClaimsQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    group_by: Some(vec![FactcheckClaimsQueryGroupBy::Model]),
                    filter: None,
                    include: Some(vec![FactcheckClaimsQueryInclude::Theme]),
                    limit: Some(0),
                    max_results: Some(0),
                    cursor: Some("".to_string()),
                })
                .send()
                .await?;
            if let Some(event) = events.next().await {
                event?;
            }
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "streamClaims".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/factcheck/claims/stream".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "getChannels", "/v2/reports/social/youtube/channels") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .social()
                .youtube()
                .get_channels(YoutubeChannelsQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    filter: None,
                    limit: None,
                    cursor: None,
                    source_types: None,
                    group_by: None,
                    interval: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "getChannels".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/social/youtube/channels".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "getChannels", "/v2/reports/social/youtube/channels") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .social()
                .youtube()
                .get_channels(YoutubeChannelsQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    filter: None,
                    limit: Some(0),
                    cursor: Some("".to_string()),
                    source_types: Some(vec![YoutubeChannelsQuerySourceType::Video]),
                    group_by: Some(vec![YoutubeChannelsQueryGroupBy::Channel]),
                    interval: Some(YoutubeChannelsQueryInterval::Day),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "getChannels".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/social/youtube/channels".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "getVideos", "/v2/reports/social/youtube/videos") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .social()
                .youtube()
                .get_videos(YoutubeVideosQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    filter: None,
                    limit: None,
                    cursor: None,
                    source_types: None,
                    attribution: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "getVideos".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/social/youtube/videos".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "getVideos", "/v2/reports/social/youtube/videos") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .social()
                .youtube()
                .get_videos(YoutubeVideosQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    filter: None,
                    limit: Some(0),
                    cursor: Some("".to_string()),
                    source_types: Some(vec![YoutubeVideosQuerySourceType::Video]),
                    attribution: Some(YoutubeVideosQueryAttribution::Attributed),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "getVideos".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/social/youtube/videos".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "getSummary", "/v2/reports/social/youtube/summary") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .reports()
                .social()
                .youtube()
                .get_summary(YoutubeSummaryQuery {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    start_date: "".to_string(),
                    end_date: "".to_string(),
                    filter: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "getSummary".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/social/youtube/summary".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "list", "/v1/content/{asset_id}/optimization") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .content()
                .optimization()
                .list("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "list".to_string(),
            method: "GET".to_string(),
            path: "/v1/content/{asset_id}/optimization".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "list", "/v1/content/{asset_id}/optimization") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .content()
                .optimization()
                .list("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .limit(10000)
                .offset(0)
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "list".to_string(),
            method: "GET".to_string(),
            path: "/v1/content/{asset_id}/optimization".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "retrieve", "/v1/content/{asset_id}/optimization/{content_id}") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .content()
                .optimization()
                .retrieve(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "retrieve".to_string(),
            method: "GET".to_string(),
            path: "/v1/content/{asset_id}/optimization/{content_id}".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "list", "/v1/agents") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client.agents().list().send().await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "list".to_string(),
            method: "GET".to_string(),
            path: "/v1/agents".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "list", "/v1/agents") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .agents()
                .list()
                .statuses(vec![ListAgentsStatusFilter::Published])
                .limit(100)
                .next_cursor("example")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "list".to_string(),
            method: "GET".to_string(),
            path: "/v1/agents".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "retrieve", "/v1/agents/{agent_id}") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .agents()
                .retrieve("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "retrieve".to_string(),
            method: "GET".to_string(),
            path: "/v1/agents/{agent_id}".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "retrieve", "/v1/agents/{agent_id}") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .agents()
                .retrieve("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .version(AgentVersion::Published)
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "retrieve".to_string(),
            method: "GET".to_string(),
            path: "/v1/agents/{agent_id}".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "create", "/v1/agents") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .agents()
                .create(CreateAgentRequest {
                    organization_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    name: "x".to_string(),
                    description: None,
                    graph: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "create".to_string(),
            method: "POST".to_string(),
            path: "/v1/agents".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "create", "/v1/agents") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .agents()
                .create(CreateAgentRequest {
                    organization_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    name: "x".to_string(),
                    description: Some("".to_string()),
                    graph: Some(serde_json::json!(null)),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "create".to_string(),
            method: "POST".to_string(),
            path: "/v1/agents".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "publish", "/v1/agents/{agent_id}/publish") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .agents()
                .publish("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "publish".to_string(),
            method: "POST".to_string(),
            path: "/v1/agents/{agent_id}/publish".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "update", "/v1/agents/{agent_id}") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .agents()
                .update(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    UpdateAgentRequest {
                        graph: serde_json::json!(null),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "update".to_string(),
            method: "PATCH".to_string(),
            path: "/v1/agents/{agent_id}".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "retrieveGraph", "/v1/agents/{agent_id}/graph") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .agents()
                .retrieve_graph("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "retrieveGraph".to_string(),
            method: "GET".to_string(),
            path: "/v1/agents/{agent_id}/graph".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "retrieveGraph", "/v1/agents/{agent_id}/graph") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .agents()
                .retrieve_graph("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .version(AgentVersion::Published)
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "retrieveGraph".to_string(),
            method: "GET".to_string(),
            path: "/v1/agents/{agent_id}/graph".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "create", "/v1/agents/{agent_id}/runs") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .agents()
                .runs()
                .create("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "create".to_string(),
            method: "POST".to_string(),
            path: "/v1/agents/{agent_id}/runs".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "retrieve", "/v1/agents/{agent_id}/runs/{run_id}") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .agents()
                .runs()
                .retrieve(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "retrieve".to_string(),
            method: "GET".to_string(),
            path: "/v1/agents/{agent_id}/runs/{run_id}".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "retrieve", "/v1/agents/{agent_id}/runs/{run_id}") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .agents()
                .runs()
                .retrieve(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                )
                .verbose(false)
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "retrieve".to_string(),
            method: "GET".to_string(),
            path: "/v1/agents/{agent_id}/runs/{run_id}".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "list", "/v1/agents/node-types") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client.agents().node_types().list().send().await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "list".to_string(),
            method: "GET".to_string(),
            path: "/v1/agents/node-types".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "retrieveSchema", "/v1/agents/node-types/{node_type}/schema") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client.agents().node_types().retrieve_schema("example").send().await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "retrieveSchema".to_string(),
            method: "GET".to_string(),
            path: "/v1/agents/node-types/{node_type}/schema".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "list", "/v1/knowledge-bases") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client.knowledge_bases().list().send().await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "list".to_string(),
            method: "GET".to_string(),
            path: "/v1/knowledge-bases".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "list", "/v1/knowledge-bases") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .knowledge_bases()
                .list()
                .organization_id("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "list".to_string(),
            method: "GET".to_string(),
            path: "/v1/knowledge-bases".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "search", "/v1/knowledge-bases/{knowledge_base_id}/search") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .knowledge_bases()
                .search(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    SearchKnowledgeBaseRequest {
                        query: "x".to_string(),
                        top_k: 0,
                        return_full_page: None,
                        filters: None,
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "search".to_string(),
            method: "POST".to_string(),
            path: "/v1/knowledge-bases/{knowledge_base_id}/search".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "search", "/v1/knowledge-bases/{knowledge_base_id}/search") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .knowledge_bases()
                .search(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    SearchKnowledgeBaseRequest {
                        query: "x".to_string(),
                        top_k: 0,
                        return_full_page: Some(false),
                        filters: Some(SearchKnowledgeBaseFilters {
                            tags: None,
                            folders: None,
                        }),
                    },
                )
                .organization_id("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "search".to_string(),
            method: "POST".to_string(),
            path: "/v1/knowledge-bases/{knowledge_base_id}/search".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "create", "/v1/knowledge-bases/{knowledge_base_id}/documents") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .knowledge_bases()
                .documents()
                .create(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    DocumentsCreateBody {
                        name: "x".to_string(),
                        text: "x".to_string(),
                        folder: None,
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "create".to_string(),
            method: "POST".to_string(),
            path: "/v1/knowledge-bases/{knowledge_base_id}/documents".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "create", "/v1/knowledge-bases/{knowledge_base_id}/documents") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .knowledge_bases()
                .documents()
                .create(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    DocumentsCreateBody {
                        name: "x".to_string(),
                        text: "x".to_string(),
                        folder: Some("x".to_string()),
                    },
                )
                .organization_id("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "create".to_string(),
            method: "POST".to_string(),
            path: "/v1/knowledge-bases/{knowledge_base_id}/documents".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "update", "/v1/knowledge-bases/{knowledge_base_id}/documents") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .knowledge_bases()
                .documents()
                .update(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    DocumentsUpdateBody {
                        name: "x".to_string(),
                        text: "x".to_string(),
                        folder: None,
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "update".to_string(),
            method: "PUT".to_string(),
            path: "/v1/knowledge-bases/{knowledge_base_id}/documents".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "update", "/v1/knowledge-bases/{knowledge_base_id}/documents") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .knowledge_bases()
                .documents()
                .update(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    DocumentsUpdateBody {
                        name: "x".to_string(),
                        text: "x".to_string(),
                        folder: Some("x".to_string()),
                    },
                )
                .organization_id("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "update".to_string(),
            method: "PUT".to_string(),
            path: "/v1/knowledge-bases/{knowledge_base_id}/documents".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "delete", "/v1/knowledge-bases/{knowledge_base_id}/documents") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .knowledge_bases()
                .documents()
                .delete(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    DeleteDocumentRequest { name: "x".to_string() },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "delete".to_string(),
            method: "DELETE".to_string(),
            path: "/v1/knowledge-bases/{knowledge_base_id}/documents".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "delete", "/v1/knowledge-bases/{knowledge_base_id}/documents") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .knowledge_bases()
                .documents()
                .delete(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    DeleteDocumentRequest { name: "x".to_string() },
                )
                .organization_id("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "delete".to_string(),
            method: "DELETE".to_string(),
            path: "/v1/knowledge-bases/{knowledge_base_id}/documents".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "create", "/v1/knowledge-bases/{knowledge_base_id}/folders") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .knowledge_bases()
                .folders()
                .create(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    AddFolderRequest { path: "x".to_string() },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "create".to_string(),
            method: "POST".to_string(),
            path: "/v1/knowledge-bases/{knowledge_base_id}/folders".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "create", "/v1/knowledge-bases/{knowledge_base_id}/folders") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .knowledge_bases()
                .folders()
                .create(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    AddFolderRequest { path: "x".to_string() },
                )
                .organization_id("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "create".to_string(),
            method: "POST".to_string(),
            path: "/v1/knowledge-bases/{knowledge_base_id}/folders".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "delete", "/v1/knowledge-bases/{knowledge_base_id}/folders") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .knowledge_bases()
                .folders()
                .delete(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    DeleteFolderRequest {
                        path: "x".to_string(),
                        recursive: None,
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "delete".to_string(),
            method: "DELETE".to_string(),
            path: "/v1/knowledge-bases/{knowledge_base_id}/folders".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "delete", "/v1/knowledge-bases/{knowledge_base_id}/folders") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .knowledge_bases()
                .folders()
                .delete(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    DeleteFolderRequest {
                        path: "x".to_string(),
                        recursive: Some(false),
                    },
                )
                .organization_id("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "delete".to_string(),
            method: "DELETE".to_string(),
            path: "/v1/knowledge-bases/{knowledge_base_id}/folders".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "list", "/v1/projects") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .projects()
                .list("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "list".to_string(),
            method: "GET".to_string(),
            path: "/v1/projects".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "list", "/v1/projects") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .projects()
                .list("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .status("example")
                .limit(100)
                .offset(0)
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "list".to_string(),
            method: "GET".to_string(),
            path: "/v1/projects".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "create", "/v1/projects") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .projects()
                .create(CreateProjectRequest {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    title: None,
                    project_name: None,
                    focus: None,
                    topics: None,
                    attachments: None,
                    generation_context: None,
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "create".to_string(),
            method: "POST".to_string(),
            path: "/v1/projects".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "create", "/v1/projects") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .projects()
                .create(CreateProjectRequest {
                    category_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    title: Some("x".to_string()),
                    project_name: Some("x".to_string()),
                    focus: Some("x".to_string()),
                    topics: Some(vec!["x".to_string()]),
                    attachments: Some(vec![ProjectAttachment {
                        id: "x".to_string(),
                        name: "x".to_string(),
                        mime_type: "x".to_string(),
                        size_bytes: 0,
                        data_base64: "x".to_string(),
                    }]),
                    generation_context: Some(ProjectGenerationContext {
                        date_range: None,
                        platforms: None,
                        project_categories: None,
                        regions: None,
                        tags: None,
                    }),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "create".to_string(),
            method: "POST".to_string(),
            path: "/v1/projects".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "retrieve", "/v1/projects/{project_id}") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .projects()
                .retrieve(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "retrieve".to_string(),
            method: "GET".to_string(),
            path: "/v1/projects/{project_id}".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "delete", "/v1/projects/{project_id}") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            client
                .projects()
                .delete(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "delete".to_string(),
            method: "DELETE".to_string(),
            path: "/v1/projects/{project_id}".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "getStatus", "/v1/projects/{project_id}/status") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .projects()
                .get_status(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "getStatus".to_string(),
            method: "GET".to_string(),
            path: "/v1/projects/{project_id}/status".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "archive", "/v1/projects/{project_id}/archive") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .projects()
                .archive(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "archive".to_string(),
            method: "POST".to_string(),
            path: "/v1/projects/{project_id}/archive".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "unarchive", "/v1/projects/{project_id}/unarchive") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .projects()
                .unarchive(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "unarchive".to_string(),
            method: "POST".to_string(),
            path: "/v1/projects/{project_id}/unarchive".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "list", "/v1/projects/generations") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .projects()
                .generations()
                .list("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "list".to_string(),
            method: "GET".to_string(),
            path: "/v1/projects/generations".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "list", "/v1/projects/generations") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .projects()
                .generations()
                .list("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .status("example")
                .limit(100)
                .offset(0)
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "list".to_string(),
            method: "GET".to_string(),
            path: "/v1/projects/generations".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "retrieve", "/v1/projects/generations/{run_id}") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .projects()
                .generations()
                .retrieve(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "retrieve".to_string(),
            method: "GET".to_string(),
            path: "/v1/projects/generations/{run_id}".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "list", "/v1/projects/{project_id}/tasks") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .projects()
                .tasks()
                .list(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "list".to_string(),
            method: "GET".to_string(),
            path: "/v1/projects/{project_id}/tasks".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "create", "/v1/projects/{project_id}/tasks") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .projects()
                .tasks()
                .create(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    CreateProjectTaskRequest {
                        title: "x".to_string(),
                        summary: None,
                        brief: None,
                        r#type: None,
                        topic: None,
                        impact: None,
                        reference_url: None,
                        reference_label: None,
                        position: None,
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "create".to_string(),
            method: "POST".to_string(),
            path: "/v1/projects/{project_id}/tasks".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "create", "/v1/projects/{project_id}/tasks") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .projects()
                .tasks()
                .create(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    CreateProjectTaskRequest {
                        title: "x".to_string(),
                        summary: Some("x".to_string()),
                        brief: Some("x".to_string()),
                        r#type: Some("x".to_string()),
                        topic: Some("x".to_string()),
                        impact: Some(0),
                        reference_url: Some("x".to_string()),
                        reference_label: Some("x".to_string()),
                        position: Some(0),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "create".to_string(),
            method: "POST".to_string(),
            path: "/v1/projects/{project_id}/tasks".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "retrieve", "/v1/projects/{project_id}/tasks/{task_id}") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .projects()
                .tasks()
                .retrieve(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "retrieve".to_string(),
            method: "GET".to_string(),
            path: "/v1/projects/{project_id}/tasks/{task_id}".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "update", "/v1/projects/{project_id}/tasks/{task_id}") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .projects()
                .tasks()
                .update(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    UpdateProjectTaskRequest {
                        title: None,
                        summary: None,
                        brief: None,
                        r#type: None,
                        topic: None,
                        impact: None,
                        reference_url: None,
                        reference_label: None,
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "update".to_string(),
            method: "PATCH".to_string(),
            path: "/v1/projects/{project_id}/tasks/{task_id}".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "update", "/v1/projects/{project_id}/tasks/{task_id}") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .projects()
                .tasks()
                .update(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    UpdateProjectTaskRequest {
                        title: Some("x".to_string()),
                        summary: Some("x".to_string()),
                        brief: Some("x".to_string()),
                        r#type: Some("x".to_string()),
                        topic: Some("x".to_string()),
                        impact: Some(0),
                        reference_url: Some("x".to_string()),
                        reference_label: Some("x".to_string()),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "update".to_string(),
            method: "PATCH".to_string(),
            path: "/v1/projects/{project_id}/tasks/{task_id}".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "delete", "/v1/projects/{project_id}/tasks/{task_id}") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            client
                .projects()
                .tasks()
                .delete(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "delete".to_string(),
            method: "DELETE".to_string(),
            path: "/v1/projects/{project_id}/tasks/{task_id}".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(
        &filter,
        "updateStatus",
        "/v1/projects/{project_id}/tasks/{task_id}/status",
    ) {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .projects()
                .tasks()
                .update_status(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    UpdateProjectTaskStatusRequest {
                        status: UpdateProjectTaskStatusRequestStatus::NotStarted,
                        note: None,
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "updateStatus".to_string(),
            method: "POST".to_string(),
            path: "/v1/projects/{project_id}/tasks/{task_id}/status".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(
        &filter,
        "updateStatus",
        "/v1/projects/{project_id}/tasks/{task_id}/status",
    ) {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .projects()
                .tasks()
                .update_status(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    UpdateProjectTaskStatusRequest {
                        status: UpdateProjectTaskStatusRequestStatus::NotStarted,
                        note: Some("x".to_string()),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "updateStatus".to_string(),
            method: "POST".to_string(),
            path: "/v1/projects/{project_id}/tasks/{task_id}/status".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "list", "/v1/integrations") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client.integrations().list().send().await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "list".to_string(),
            method: "GET".to_string(),
            path: "/v1/integrations".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "list", "/v1/integrations") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .integrations()
                .list()
                .organization_id("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .provider("example")
                .status_filter("active".to_string())
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "list".to_string(),
            method: "GET".to_string(),
            path: "/v1/integrations".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "create", "/v1/documents") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .documents()
                .create(CreateDocumentRequest {
                    id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    organization_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                    name: "x".to_string(),
                    content_markdown: "x".to_string(),
                })
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "create".to_string(),
            method: "POST".to_string(),
            path: "/v1/documents".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "list", "/v1/documents") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .documents()
                .list("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "list".to_string(),
            method: "GET".to_string(),
            path: "/v1/documents".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "list", "/v1/documents") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .documents()
                .list("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .q("example")
                .sort("example")
                .limit(20)
                .next_cursor("example")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "list".to_string(),
            method: "GET".to_string(),
            path: "/v1/documents".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "retrieve", "/v1/documents/{document_id}") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .documents()
                .retrieve(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "retrieve".to_string(),
            method: "GET".to_string(),
            path: "/v1/documents/{document_id}".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "retrieve", "/v1/documents/{document_id}") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .documents()
                .retrieve(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                )
                .include_tabs(true)
                .include_comments(true)
                .preview(true)
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "retrieve".to_string(),
            method: "GET".to_string(),
            path: "/v1/documents/{document_id}".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "update", "/v1/documents/{document_id}") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .documents()
                .update(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    UpdateDocumentRequest {
                        organization_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                        name: None,
                        visibility: None,
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "update".to_string(),
            method: "PATCH".to_string(),
            path: "/v1/documents/{document_id}".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "update", "/v1/documents/{document_id}") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .documents()
                .update(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    UpdateDocumentRequest {
                        organization_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                        name: Some("x".to_string()),
                        visibility: Some(UpdateDocumentRequestVisibility::InvitedOnly),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "update".to_string(),
            method: "PATCH".to_string(),
            path: "/v1/documents/{document_id}".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "delete", "/v1/documents/{document_id}") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            client
                .documents()
                .delete(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "delete".to_string(),
            method: "DELETE".to_string(),
            path: "/v1/documents/{document_id}".to_string(),
            label: "".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "replaceContent", "/v1/documents/{document_id}/content") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .documents()
                .replace_content(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    ReplaceDocumentContentRequest {
                        organization_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                        content_markdown: "".to_string(),
                        skip_title_sync: None,
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "replaceContent".to_string(),
            method: "POST".to_string(),
            path: "/v1/documents/{document_id}/content".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "replaceContent", "/v1/documents/{document_id}/content") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .documents()
                .replace_content(
                    "7c9e6679-7425-40de-944b-e07fc1f90ae7",
                    ReplaceDocumentContentRequest {
                        organization_id: "7c9e6679-7425-40de-944b-e07fc1f90ae7".to_string(),
                        content_markdown: "".to_string(),
                        skip_title_sync: Some(false),
                    },
                )
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "replaceContent".to_string(),
            method: "POST".to_string(),
            path: "/v1/documents/{document_id}/content".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "retrieveInsights", "/v1/ads/openai-ads/ad-account/insights") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .ads()
                .openai_ads()
                .ad_account()
                .retrieve_insights()
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "retrieveInsights".to_string(),
            method: "GET".to_string(),
            path: "/v1/ads/openai-ads/ad-account/insights".to_string(),
            label: "required params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "retrieveInsights", "/v1/ads/openai-ads/ad-account/insights") {
        let started = std::time::Instant::now();
        let result: Result<(), Error> = async {
            let _ = client
                .ads()
                .openai_ads()
                .ad_account()
                .retrieve_insights()
                .organization_id("7c9e6679-7425-40de-944b-e07fc1f90ae7")
                .aggregation_level("ad_account".to_string())
                .time_granularity("hourly".to_string())
                .time_ranges(vec!["timeRange".to_string()])
                .limit(1)
                .after("example")
                .before("example")
                .send()
                .await?;
            Ok(())
        }
        .await;
        let duration_ms = started.elapsed().as_millis() as i64;
        let (status, error) = match result {
            Ok(()) => ("passed", String::new()),
            Err(error) if is_smoke_failure(&error) => ("failed", format!("{error}")),
            // A response came back (API error or decode mismatch): the request
            // reached the server, which is what this smoke verifies.
            Err(_) => ("passed", String::new()),
        };
        results.push(SmokeResult {
            operation: "retrieveInsights".to_string(),
            method: "GET".to_string(),
            path: "/v1/ads/openai-ads/ad-account/insights".to_string(),
            label: "all params".to_string(),
            status: status.to_string(),
            duration_ms,
            error,
        });
    }
    if selected(&filter, "getBotsReportV2", "/v2/reports/bots") {
        results.push(SmokeResult {
            operation: "getBotsReportV2".to_string(),
            method: "POST".to_string(),
            path: "/v2/reports/bots".to_string(),
            label: String::new(),
            status: "skipped".to_string(),
            duration_ms: 0,
            error: "required request arguments could not be synthesized into a compiling value".to_string(),
        });
    }

    let passed = results.iter().filter(|result| result.status == "passed").count();
    let failed = results.iter().filter(|result| result.status == "failed").count();
    let report = SmokeReport {
        total: results.len(),
        failed,
        results,
    };

    if let Some(path) = report_path {
        let json = serde_json::to_string(&report).expect("serialize smoke report");
        std::fs::write(&path, json).expect("write smoke report");
    } else {
        for result in &report.results {
            match result.status.as_str() {
                "passed" => println!("PASS {} ({} {})", result.operation, result.method, result.path),
                "skipped" => println!(
                    "SKIP {} ({} {}): {}",
                    result.operation, result.method, result.path, result.error
                ),
                _ => eprintln!(
                    "FAIL {} ({} {})\n{}",
                    result.operation, result.method, result.path, result.error
                ),
            }
        }
    }

    // A vacuous run (nothing passed: empty SDK or every operation skipped) fails, like the
    // shared runner. Skipped operations on their own never fail the run.
    if report.failed > 0 || passed == 0 {
        std::process::exit(1);
    }
}

/// A smoke failure is a plumbing problem — the request could not be sent or
/// the client was misconfigured. An API error (a response with a non-success
/// status) or a decode error means the request reached the server and came
/// back, which is what this smoke verifies; the synthetic response body the
/// mock returns is not the SDK's concern.
fn is_smoke_failure(error: &Error) -> bool {
    matches!(
        error,
        Error::Transport(_) | Error::Config(_) | Error::MissingParameter(_)
    )
}
