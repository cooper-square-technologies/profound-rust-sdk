// Code generated by Scalar SDK Generator. DO NOT EDIT.
//! Generated request and response models declared under `factcheck`.

#[allow(unused_imports)]
use super::*;
use serde::{Deserialize, Serialize};

///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct CitationScoreRef {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub citation_category: Option<String>,
}

/// An accuracy score row: the headline (no dims), a daily point, or a per-dimension breakdown.
///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct FactcheckScoreRow {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub date: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub model: Option<DimensionRef>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub region: Option<DimensionRef>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub persona: Option<DimensionRef>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub prompt: Option<DimensionRef>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub topic: Option<DimensionRef>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tag: Option<DimensionRef>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub theme: Option<DimensionRef>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub citation: Option<CitationScoreRef>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub accuracy: Option<f64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub accurate: Option<i64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub inaccurate: Option<i64>,
}

///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct FactcheckScoresInfo {
    /// Total rows matching the query before pagination (null when not computed).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub total_results: Option<i64>,
    /// Number of rows returned in `data` for this page.
    pub count: i64,
    /// Opaque cursor for the next page; null on the last page.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_cursor: Option<String>,
    /// Display names of the models the report covers.
    pub models: Vec<String>,
    /// Echoed request start date (YYYY-MM-DD, ET).
    pub start_date: String,
    /// Echoed request end date (YYYY-MM-DD, ET).
    pub end_date: String,
    /// Echoed normalized filter tree, or null when no filter was sent.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub filter: Option<serde_json::Value>,
    /// Dimensions the scores are sliced by (empty → headline).
    pub group_by: Vec<String>,
}

/// Accuracy scores. `group_by` picks the slice (one or two dimensions); empty → the headline score.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct FactcheckScoresQuery {
    pub category_id: String,
    /// YYYY-MM-DD, ET, inclusive
    pub start_date: String,
    /// YYYY-MM-DD, ET, inclusive
    pub end_date: String,
    /// Up to two dimensions to slice by; empty returns the headline score. `citation` must be alone.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_by: Option<Vec<FactcheckScoresQueryGroupBy>>,
    /// Scope which responses count (see Filtering).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub filter: Option<FilterNode>,
    /// Rows per page; default 100.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
    /// Stream only: cap rows returned.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub max_results: Option<i64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cursor: Option<String>,
}

impl FactcheckScoresQuery {
    /// Creates a new `FactcheckScoresQuery` from its required fields, leaving every optional one unset.
    ///
    /// Pair it with struct-update syntax to set only the optionals you need:
    /// `FactcheckScoresQuery { group_by: Some(…), ..FactcheckScoresQuery::new(category_id, start_date, end_date) }`.
    pub fn new(category_id: impl Into<String>, start_date: impl Into<String>, end_date: impl Into<String>) -> Self {
        Self {
            category_id: category_id.into(),
            start_date: start_date.into(),
            end_date: end_date.into(),
            group_by: None,
            filter: None,
            limit: None,
            max_results: None,
            cursor: None,
        }
    }
}

///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct FactcheckScoresResponse {
    pub info: FactcheckScoresInfo,
    pub data: Vec<FactcheckScoreRow>,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
#[non_exhaustive]
pub enum FactcheckStreamScoresResponse {
    FactcheckScoresInfo(FactcheckScoresInfo),
    FactcheckScoreRow(FactcheckScoreRow),
    /// A variant not recognized by this version of the SDK, preserved verbatim.
    ///
    /// Match the specific variants first. Matching this one directly is an
    /// anti-pattern: that arm silently stops matching once a future SDK
    /// release adds the real variant.
    #[serde(untagged)]
    Unknown(serde_json::Value),
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum FactcheckScoresQueryGroupBy {
    #[serde(rename = "date")]
    Date,
    #[serde(rename = "model")]
    Model,
    #[serde(rename = "region")]
    Region,
    #[serde(rename = "persona")]
    Persona,
    #[serde(rename = "prompt")]
    Prompt,
    #[serde(rename = "topic")]
    Topic,
    #[serde(rename = "tag")]
    Tag,
    #[serde(rename = "citation")]
    Citation,
    #[serde(rename = "theme")]
    Theme,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl FactcheckScoresQueryGroupBy {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Date => "date",
            Self::Model => "model",
            Self::Region => "region",
            Self::Persona => "persona",
            Self::Prompt => "prompt",
            Self::Topic => "topic",
            Self::Tag => "tag",
            Self::Citation => "citation",
            Self::Theme => "theme",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for FactcheckScoresQueryGroupBy {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for FactcheckScoresQueryGroupBy {
    fn from(value: &str) -> Self {
        match value {
            "date" => Self::Date,
            "model" => Self::Model,
            "region" => Self::Region,
            "persona" => Self::Persona,
            "prompt" => Self::Prompt,
            "topic" => Self::Topic,
            "tag" => Self::Tag,
            "citation" => Self::Citation,
            "theme" => Self::Theme,
            other => Self::Unknown(other.to_owned()),
        }
    }
}
