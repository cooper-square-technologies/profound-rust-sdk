// Code generated by Scalar SDK Generator. DO NOT EDIT.
//! Generated request and response models declared under `categories`.

#[allow(unused_imports)]
use super::*;
use serde::{Deserialize, Serialize};

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum AnalysisType {
    #[serde(rename = "visibility")]
    Visibility,
    #[serde(rename = "sentiment")]
    Sentiment,
    #[serde(rename = "sentiment_v2")]
    SentimentV2,
    #[serde(rename = "accuracy")]
    Accuracy,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl AnalysisType {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Visibility => "visibility",
            Self::Sentiment => "sentiment",
            Self::SentimentV2 => "sentiment_v2",
            Self::Accuracy => "accuracy",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for AnalysisType {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for AnalysisType {
    fn from(value: &str) -> Self {
        match value {
            "visibility" => Self::Visibility,
            "sentiment" => Self::Sentiment,
            "sentiment_v2" => Self::SentimentV2,
            "accuracy" => Self::Accuracy,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct CategoryAsset {
    pub id: String,
    pub name: String,
    pub website: String,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub alternate_domains: Option<Vec<String>>,
    pub is_owned: bool,
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    pub logo_url: String,
}

///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct CategoryPersona {
    pub id: String,
    pub name: String,
    pub persona: PersonaProfile,
}

///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct CategoryPersonasResponse {
    pub data: Vec<CategoryPersona>,
}

///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct CategoryPromptsResponse {
    pub info: PaginationInfo,
    pub data: Vec<Prompt>,
}

/// A category annotated with the organization that owns it.
///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct CategoryWithOrganization {
    pub id: String,
    pub name: String,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub internal_name: Option<String>,
    pub organization: Organization,
}

///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct CitationCategoriesResponse {
    pub data: Vec<CitationCategory>,
}

///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct CitationCategory {
    /// Value to pass to the v2 citations `citation_category` filter.
    pub value: String,
    /// Display name.
    pub name: String,
}

/// A single prompt to create within a category.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct CreatePromptInput {
    /// Optional client-generated UUID for the prompt. When provided, creation is idempotent: retrying a request with the same id will not create a duplicate prompt. Omit to have the server generate one (non-idempotent).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// The prompt text to be sent to AI platforms.
    pub prompt: String,
    /// Topic to assign. A new topic is created if the name doesn't exist.
    pub topic: IdOrName,
    /// Language code (e.g. 'en-US')
    pub language: String,
    /// Tags to assign. New tags are created if names don't exist.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tags: Option<Vec<IdOrName>>,
    /// Regions where the prompt will be collected.
    pub regions: Vec<IdOrName>,
    /// AI platforms where the prompt will be collected.
    pub platforms: Vec<IdOrName>,
    /// Personas to use when collecting. Omit for default (no persona).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub personas: Option<Vec<IdOrName>>,
    /// Analysis types: 'visibility', 'sentiment', 'accuracy'. Defaults to \['visibility'\].
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub analysis_types: Option<Vec<CreatePromptInputAnalysisType>>,
    /// Deprecated. Use analysis_types instead. 'Visibility' or 'Sentiment'.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub prompt_type: Option<String>,
    /// Required for Sentiment prompts. The brand asset to evaluate.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub asset: Option<IdOrName>,
}

impl CreatePromptInput {
    /// Creates a new `CreatePromptInput` from its required fields, leaving every optional one unset.
    ///
    /// Pair it with struct-update syntax to set only the optionals you need:
    /// `CreatePromptInput { id: Some(…), ..CreatePromptInput::new(prompt, topic, language, regions, platforms) }`.
    pub fn new(
        prompt: impl Into<String>,
        topic: IdOrName,
        language: impl Into<String>,
        regions: Vec<IdOrName>,
        platforms: Vec<IdOrName>,
    ) -> Self {
        Self {
            id: None,
            prompt: prompt.into(),
            topic,
            language: language.into(),
            tags: None,
            regions,
            platforms,
            personas: None,
            analysis_types: None,
            prompt_type: None,
            asset: None,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct CreatePromptsBody {
    /// List of prompts to create.
    pub prompts: Vec<CreatePromptInput>,
    /// When true, validate and preview changes without persisting them.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dry_run: Option<bool>,
}

impl CreatePromptsBody {
    /// Creates a new `CreatePromptsBody` from its required fields, leaving every optional one unset.
    ///
    /// Pair it with struct-update syntax to set only the optionals you need:
    /// `CreatePromptsBody { dry_run: Some(…), ..CreatePromptsBody::new(prompts) }`.
    pub fn new(prompts: Vec<CreatePromptInput>) -> Self {
        Self { prompts, dry_run: None }
    }
}

/// Response from creating prompts.
///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct CreatePromptsResponse {
    /// Whether this was a dry run (no changes persisted).
    pub dry_run: bool,
    /// Number of prompts created.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created: Option<i64>,
    /// Number of new topics created.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub topics_created: Option<i64>,
    /// Number of new tags created.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tags_created: Option<i64>,
    /// List of created (or previewed) prompts with resolved references.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub prompts: Option<Vec<PromptPreview>>,
}

/// Shows the old and new value for a changed field.
///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct FieldDiff {
    /// Previous value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub old: Option<serde_json::Value>,
    /// New value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub new: Option<serde_json::Value>,
}

/// Reference by id, name, or both. Plain strings work too:
/// UUIDs become id lookups, other strings become name lookups.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
pub struct IdOrName {
    /// UUID of the resource.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// Display name of the resource.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
}

/// Shows which resources were added or removed.
///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct NamedResourceDiffList {
    /// Resources that were added.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub added: Option<Vec<NamedResource>>,
    /// Resources that were removed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub removed: Option<Vec<NamedResource>>,
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum OrderByDirection {
    #[serde(rename = "asc")]
    Asc,
    #[serde(rename = "desc")]
    Desc,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl OrderByDirection {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Asc => "asc",
            Self::Desc => "desc",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for OrderByDirection {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for OrderByDirection {
    fn from(value: &str) -> Self {
        match value {
            "asc" => Self::Asc,
            "desc" => Self::Desc,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct PaginationInfo {
    pub total_rows: i64,
    pub limit: i64,
    #[serde(default)]
    pub next_cursor: Option<String>,
}

///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct Prompt {
    pub id: String,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub analysis_types: Option<Vec<PromptAnalysisType>>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub prompt_type: Option<String>,
    pub prompt: String,
    pub language: String,
    pub status: PromptStatus,
    /// Generic id+name reference used across domain boundaries.
    pub topic: NamedResource,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tags: Option<Vec<NamedResource>>,
    pub regions: Vec<NamedResource>,
    pub platforms: Vec<NamedResource>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub personas: Option<Vec<NamedResource>>,
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    pub updated_at: chrono::DateTime<chrono::FixedOffset>,
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum PromptOrderBy {
    #[serde(rename = "created_at")]
    CreatedAt,
    #[serde(rename = "prompt")]
    Prompt,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl PromptOrderBy {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::CreatedAt => "created_at",
            Self::Prompt => "prompt",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for PromptOrderBy {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for PromptOrderBy {
    fn from(value: &str) -> Self {
        match value {
            "created_at" => Self::CreatedAt,
            "prompt" => Self::Prompt,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

/// Preview of a prompt that was (or would be) created.
///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct PromptPreview {
    /// Generated prompt ID.
    pub id: String,
    /// The prompt text.
    pub prompt: String,
    /// Resolved topic.
    pub topic: NamedResource,
    /// Language code.
    pub language: String,
    /// Resolved tags.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tags: Option<Vec<NamedResource>>,
    /// Resolved regions.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub regions: Option<Vec<NamedResource>>,
    /// Resolved platforms.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub platforms: Option<Vec<NamedResource>>,
    /// Resolved personas.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub personas: Option<Vec<NamedResource>>,
    /// Analysis types assigned to this prompt.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub analysis_types: Option<Vec<PromptPreviewAnalysisType>>,
    /// Resolved asset, if applicable.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub asset: Option<NamedResource>,
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum PromptType {
    #[serde(rename = "visibility")]
    Visibility,
    #[serde(rename = "sentiment")]
    Sentiment,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl PromptType {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Visibility => "visibility",
            Self::Sentiment => "sentiment",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for PromptType {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for PromptType {
    fn from(value: &str) -> Self {
        match value {
            "visibility" => Self::Visibility,
            "sentiment" => Self::Sentiment,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

/// Preview of changes applied (or to be applied) to a single prompt. Only changed fields are included.
///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct PromptUpdatePreview {
    /// ID of the updated prompt.
    pub id: String,
    /// Prompt text diff.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub prompt: Option<FieldDiff>,
    /// Topic diff.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub topic: Option<FieldDiff>,
    /// Language diff.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub language: Option<FieldDiff>,
    /// Tags added and removed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tags: Option<NamedResourceDiffList>,
    /// Regions added and removed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub regions: Option<NamedResourceDiffList>,
    /// Platforms added and removed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub platforms: Option<NamedResourceDiffList>,
    /// Personas added and removed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub personas: Option<NamedResourceDiffList>,
    /// Analysis types diff.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub analysis_types: Option<FieldDiff>,
    /// Asset diff.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub asset: Option<FieldDiff>,
}

///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct Tag {
    pub id: String,
    pub name: String,
}

///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct Topic {
    pub id: String,
    pub name: String,
    pub status: TopicStatus,
}

/// Fields to update on an existing prompt. Only provided fields are changed; omitted fields are left unchanged.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct UpdatePromptInput {
    /// ID of the prompt to update.
    pub id: String,
    /// New prompt text.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub prompt: Option<String>,
    /// New topic. A new topic is created if the name doesn't exist.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub topic: Option<IdOrName>,
    /// New language code. Must be enabled for the organization.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub language: Option<String>,
    /// New tag set. Replaces all existing tags on the prompt.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tags: Option<Vec<IdOrName>>,
    /// New region set. Replaces all existing regions.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub regions: Option<Vec<IdOrName>>,
    /// New platform set. Replaces all existing platforms.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub platforms: Option<Vec<IdOrName>>,
    /// New persona set. Replaces all existing personas.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub personas: Option<Vec<IdOrName>>,
    /// New analysis types. Replaces all existing analysis types on the prompt.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub analysis_types: Option<Vec<UpdatePromptInputAnalysisType>>,
    /// Deprecated. Use analysis_types instead.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub prompt_type: Option<String>,
    /// Asset reference. Required when analysis_types includes 'sentiment'.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub asset: Option<IdOrName>,
}

impl UpdatePromptInput {
    /// Creates a new `UpdatePromptInput` from its required fields, leaving every optional one unset.
    ///
    /// Pair it with struct-update syntax to set only the optionals you need:
    /// `UpdatePromptInput { prompt: Some(…), ..UpdatePromptInput::new(id) }`.
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            prompt: None,
            topic: None,
            language: None,
            tags: None,
            regions: None,
            platforms: None,
            personas: None,
            analysis_types: None,
            prompt_type: None,
            asset: None,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct UpdatePromptStatusBody {
    /// IDs of the prompts to update.
    pub prompt_ids: Vec<String>,
    /// Target status: 'active', 'disabled', or 'deleted'.
    pub status: UpdatePromptStatusBodyStatus,
    /// When true, validate and preview changes without persisting them.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dry_run: Option<bool>,
}

impl UpdatePromptStatusBody {
    /// Creates a new `UpdatePromptStatusBody` from its required fields, leaving every optional one unset.
    ///
    /// Pair it with struct-update syntax to set only the optionals you need:
    /// `UpdatePromptStatusBody { dry_run: Some(…), ..UpdatePromptStatusBody::new(prompt_ids, status) }`.
    pub fn new(prompt_ids: Vec<String>, status: UpdatePromptStatusBodyStatus) -> Self {
        Self {
            prompt_ids,
            status,
            dry_run: None,
        }
    }
}

/// Response from updating prompt statuses.
///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct UpdatePromptStatusResponse {
    /// Whether this was a dry run (no changes persisted).
    pub dry_run: bool,
    /// Number of prompts whose status was changed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub updated_prompts: Option<i64>,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct UpdatePromptsBody {
    /// List of prompt updates. Each entry must include an `id` and at least one field to change.
    pub prompts: Vec<UpdatePromptInput>,
    /// When true, validate and preview changes without persisting them.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dry_run: Option<bool>,
}

impl UpdatePromptsBody {
    /// Creates a new `UpdatePromptsBody` from its required fields, leaving every optional one unset.
    ///
    /// Pair it with struct-update syntax to set only the optionals you need:
    /// `UpdatePromptsBody { dry_run: Some(…), ..UpdatePromptsBody::new(prompts) }`.
    pub fn new(prompts: Vec<UpdatePromptInput>) -> Self {
        Self { prompts, dry_run: None }
    }
}

/// Response from updating prompts.
///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct UpdatePromptsResponse {
    /// Whether this was a dry run (no changes persisted).
    pub dry_run: bool,
    /// Number of prompts that had changes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub updated: Option<i64>,
    /// Number of new topics created.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub topics_created: Option<i64>,
    /// Number of new tags created.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tags_created: Option<i64>,
    /// List of prompts with their change diffs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub prompts: Option<Vec<PromptUpdatePreview>>,
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum CreatePromptInputAnalysisType {
    #[serde(rename = "visibility")]
    Visibility,
    #[serde(rename = "sentiment")]
    Sentiment,
    #[serde(rename = "sentiment_v2")]
    SentimentV2,
    #[serde(rename = "accuracy")]
    Accuracy,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl CreatePromptInputAnalysisType {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Visibility => "visibility",
            Self::Sentiment => "sentiment",
            Self::SentimentV2 => "sentiment_v2",
            Self::Accuracy => "accuracy",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for CreatePromptInputAnalysisType {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for CreatePromptInputAnalysisType {
    fn from(value: &str) -> Self {
        match value {
            "visibility" => Self::Visibility,
            "sentiment" => Self::Sentiment,
            "sentiment_v2" => Self::SentimentV2,
            "accuracy" => Self::Accuracy,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum PromptAnalysisType {
    #[serde(rename = "visibility")]
    Visibility,
    #[serde(rename = "sentiment")]
    Sentiment,
    #[serde(rename = "sentiment_v2")]
    SentimentV2,
    #[serde(rename = "accuracy")]
    Accuracy,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl PromptAnalysisType {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Visibility => "visibility",
            Self::Sentiment => "sentiment",
            Self::SentimentV2 => "sentiment_v2",
            Self::Accuracy => "accuracy",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for PromptAnalysisType {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for PromptAnalysisType {
    fn from(value: &str) -> Self {
        match value {
            "visibility" => Self::Visibility,
            "sentiment" => Self::Sentiment,
            "sentiment_v2" => Self::SentimentV2,
            "accuracy" => Self::Accuracy,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum PromptStatus {
    #[serde(rename = "active")]
    Active,
    #[serde(rename = "disabled")]
    Disabled,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl PromptStatus {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Active => "active",
            Self::Disabled => "disabled",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for PromptStatus {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for PromptStatus {
    fn from(value: &str) -> Self {
        match value {
            "active" => Self::Active,
            "disabled" => Self::Disabled,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum PromptPreviewAnalysisType {
    #[serde(rename = "visibility")]
    Visibility,
    #[serde(rename = "sentiment")]
    Sentiment,
    #[serde(rename = "sentiment_v2")]
    SentimentV2,
    #[serde(rename = "accuracy")]
    Accuracy,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl PromptPreviewAnalysisType {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Visibility => "visibility",
            Self::Sentiment => "sentiment",
            Self::SentimentV2 => "sentiment_v2",
            Self::Accuracy => "accuracy",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for PromptPreviewAnalysisType {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for PromptPreviewAnalysisType {
    fn from(value: &str) -> Self {
        match value {
            "visibility" => Self::Visibility,
            "sentiment" => Self::Sentiment,
            "sentiment_v2" => Self::SentimentV2,
            "accuracy" => Self::Accuracy,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum TopicStatus {
    #[serde(rename = "active")]
    Active,
    #[serde(rename = "disabled")]
    Disabled,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl TopicStatus {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Active => "active",
            Self::Disabled => "disabled",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for TopicStatus {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for TopicStatus {
    fn from(value: &str) -> Self {
        match value {
            "active" => Self::Active,
            "disabled" => Self::Disabled,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum UpdatePromptInputAnalysisType {
    #[serde(rename = "visibility")]
    Visibility,
    #[serde(rename = "sentiment")]
    Sentiment,
    #[serde(rename = "sentiment_v2")]
    SentimentV2,
    #[serde(rename = "accuracy")]
    Accuracy,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl UpdatePromptInputAnalysisType {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Visibility => "visibility",
            Self::Sentiment => "sentiment",
            Self::SentimentV2 => "sentiment_v2",
            Self::Accuracy => "accuracy",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for UpdatePromptInputAnalysisType {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for UpdatePromptInputAnalysisType {
    fn from(value: &str) -> Self {
        match value {
            "visibility" => Self::Visibility,
            "sentiment" => Self::Sentiment,
            "sentiment_v2" => Self::SentimentV2,
            "accuracy" => Self::Accuracy,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum UpdatePromptStatusBodyStatus {
    #[serde(rename = "active")]
    Active,
    #[serde(rename = "disabled")]
    Disabled,
    #[serde(rename = "deleted")]
    Deleted,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl UpdatePromptStatusBodyStatus {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Active => "active",
            Self::Disabled => "disabled",
            Self::Deleted => "deleted",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for UpdatePromptStatusBodyStatus {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for UpdatePromptStatusBodyStatus {
    fn from(value: &str) -> Self {
        match value {
            "active" => Self::Active,
            "disabled" => Self::Disabled,
            "deleted" => Self::Deleted,
            other => Self::Unknown(other.to_owned()),
        }
    }
}
