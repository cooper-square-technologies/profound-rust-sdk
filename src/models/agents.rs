// Code generated by Scalar SDK Generator. DO NOT EDIT.
//! Generated request and response models declared under `agents`.

#[allow(unused_imports)]
use super::*;
use serde::{Deserialize, Serialize};

/// Summary information for an agent.
///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct Agent {
    /// Unique ID for the agent.
    pub id: String,
    /// Unique ID of the organization that owns the agent.
    pub organization_id: String,
    /// Display name of the agent.
    pub name: String,
    /// Current status of the agent.
    pub status: AgentStatus,
    /// When the agent was created.
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    /// Short description of the agent, if provided.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
}

/// Detailed information for an agent.
///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct AgentDetail {
    /// Unique ID for the agent.
    pub id: String,
    /// Unique ID of the organization that owns the agent.
    pub organization_id: String,
    /// Display name of the agent.
    pub name: String,
    /// Current status of the agent.
    pub status: AgentStatus,
    /// When the agent was created.
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    /// Short description of the agent, if provided.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// Input and output schemas for this agent. Omitted while the agent's graph cannot be previewed (e.g. an empty or structurally-invalid draft); check `validation` to see what needs fixing.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema: Option<AgentSchema>,
    /// Validation report for the agent's graph, when available. Use `validation.valid` to check the agent is publishable.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub validation: Option<AgentValidation>,
}

/// An agent version's workflow graph in the canonical dialect.
///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct AgentGraph {
    /// Unique ID of the agent the graph belongs to.
    pub agent_id: String,
    /// Which version of the agent this graph is — `published` or `draft`.
    pub version: AgentVersion,
    /// Workflow graph (`{nodes, edges}`) in the canonical dialect — the same shape `create` and `update` accept. Treat it as an opaque object: it is returned verbatim, so tool-backed nodes appear in their lowered `tool` form rather than the friendly v1 node types. Read it back to copy and edit a known-good agent.
    pub graph: serde_json::Value,
}

/// Schema metadata for an agent.
///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct AgentSchema {
    /// JSON Schema for the agent's `inputs` object. Use the top-level property keys as input field names when starting a run.
    pub input: serde_json::Value,
    /// JSON Schema for the `outputs` object returned by `GET /v1/agents/{agent_id}/runs/{run_id}`.
    pub output: serde_json::Value,
}

/// Current availability status for an agent.
///
/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown_(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum AgentStatus {
    #[serde(rename = "draft")]
    Draft,
    #[serde(rename = "published")]
    Published,
    #[serde(rename = "unknown")]
    Unknown,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown_(String),
}

impl AgentStatus {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Draft => "draft",
            Self::Published => "published",
            Self::Unknown => "unknown",
            Self::Unknown_(value) => value,
        }
    }
}

impl std::fmt::Display for AgentStatus {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for AgentStatus {
    fn from(value: &str) -> Self {
        match value {
            "draft" => Self::Draft,
            "published" => Self::Published,
            "unknown" => Self::Unknown,
            other => Self::Unknown_(other.to_owned()),
        }
    }
}

/// Result of validating an agent's graph.
///
/// Mirrors the report computed on every read, so callers can confirm a draft
/// is publishable before calling publish.
///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct AgentValidation {
    /// Whether the agent's graph is valid and ready to publish.
    pub valid: bool,
    /// Problems found while validating the graph. Empty when `valid` is true.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub issues: Option<Vec<AgentValidationIssue>>,
}

/// A single problem found while validating an agent's graph.
///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct AgentValidationIssue {
    /// Stable machine-readable identifier for the kind of issue.
    pub code: String,
    /// Human-readable description of the issue.
    pub message: String,
    /// ID of the node the issue applies to, if node-specific.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_id: Option<String>,
    /// Display title of the affected node, if available.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_title: Option<String>,
    /// Name of the offending field on the node, if field-specific.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub field: Option<String>,
    /// Display title of the affected field, if available.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub field_title: Option<String>,
    /// The specific constraint that was violated, if available.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub violation: Option<String>,
}

/// Version selector for retrieving a specific agent.
///
/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum AgentVersion {
    #[serde(rename = "published")]
    Published,
    #[serde(rename = "draft")]
    Draft,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl AgentVersion {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Published => "published",
            Self::Draft => "draft",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for AgentVersion {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for AgentVersion {
    fn from(value: &str) -> Self {
        match value {
            "published" => Self::Published,
            "draft" => Self::Draft,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

/// Request body for creating a draft agent.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct CreateAgentRequest {
    /// ID of the organization that will own the agent. Required — Profound API keys are user-scoped, so the owning organization must be chosen explicitly. The caller must be a member of this organization.
    pub organization_id: String,
    /// Display name for the agent. Must be non-empty.
    pub name: String,
    /// Short description of the agent.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// Initial workflow graph for the agent's draft version. Optional — an agent can be created empty and have its graph filled in later.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub graph: Option<serde_json::Value>,
}

impl CreateAgentRequest {
    /// Creates a new `CreateAgentRequest` from its required fields, leaving every optional one unset.
    ///
    /// Pair it with struct-update syntax to set only the optionals you need:
    /// `CreateAgentRequest { description: Some(…), ..CreateAgentRequest::new(organization_id, name) }`.
    pub fn new(organization_id: impl Into<String>, name: impl Into<String>) -> Self {
        Self {
            organization_id: organization_id.into(),
            name: name.into(),
            description: None,
            graph: None,
        }
    }
}

/// Paginated list of agents.
///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct ListAgentsResponse {
    /// Agents returned for this page.
    pub data: Vec<Agent>,
    /// Cursor pagination details for this response.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pagination: Option<CursorPagination>,
}

/// Statuses you can use to filter the list agents response.
///
/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum ListAgentsStatusFilter {
    #[serde(rename = "published")]
    Published,
    #[serde(rename = "draft")]
    Draft,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl ListAgentsStatusFilter {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Published => "published",
            Self::Draft => "draft",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for ListAgentsStatusFilter {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for ListAgentsStatusFilter {
    fn from(value: &str) -> Self {
        match value {
            "published" => Self::Published,
            "draft" => Self::Draft,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

/// Request body for updating a draft agent's graph in place.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct UpdateAgentRequest {
    /// New workflow graph for the agent's draft version. Replaces the current draft graph; the agent is iterated in place rather than re-created, so its ID is stable. Required — a null graph is rejected as a 422 here rather than as a relayed upstream error.
    pub graph: serde_json::Value,
}
