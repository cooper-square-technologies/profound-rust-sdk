// Code generated by Scalar SDK Generator. DO NOT EDIT.
//! Generated request and response models declared under `web_search_results`.

#[allow(unused_imports)]
use super::*;
use serde::{Deserialize, Serialize};

/// Filter by web-search query string.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct SearchQueryFilter {
    #[serde(default, skip_serializing)]
    pub field: String,
    pub operator: SearchQueryFilterOperator,
    pub value: SearchQueryFilterValue,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct StreamWebSearchResultsQuery {
    /// Date interval for the report. (only used with date dimension)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub date_interval: Option<StreamWebSearchResultsQueryDateInterval>,
    /// Dimensions to group the report by.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dimensions: Option<Vec<StreamWebSearchResultsQueryDimension>>,
    /// Metrics to include. `search_share` is the per-prompt occurrence rate.
    pub metrics: Vec<StreamWebSearchResultsQueryMetric>,
    ///
    /// ```text
    /// Custom ordering of the report results.
    ///
    /// The order is a record of key-value pairs where:
    /// - `key` is the field to order by, which can be a metric or dimension
    /// - `value` is the direction of the order, either `asc` for ascending or `desc` for descending.
    ///
    /// When not specified, the default order is the first metric in the query descending.
    /// ```
    ///             
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_by: Option<std::collections::HashMap<String, StreamWebSearchResultsQueryOrderBy>>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pagination: Option<Pagination>,
    pub category_id: String,
    /// Start date for the report. Accepts formats: YYYY-MM-DD, YYYY-MM-DD HH:MM, or full ISO timestamp.
    pub start_date: chrono::DateTime<chrono::FixedOffset>,
    /// End date for the report. Accepts formats: YYYY-MM-DD, YYYY-MM-DD HH:MM, or full ISO timestamp.
    pub end_date: chrono::DateTime<chrono::FixedOffset>,
    /// List of filters to apply to the web search results report.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub filters: Option<Vec<StreamWebSearchResultsQueryFilter>>,
}

impl StreamWebSearchResultsQuery {
    /// Creates a new `StreamWebSearchResultsQuery` from its required fields, leaving every optional one unset.
    ///
    /// Pair it with struct-update syntax to set only the optionals you need:
    /// `StreamWebSearchResultsQuery { date_interval: Some(…), ..StreamWebSearchResultsQuery::new(…) }`.
    pub fn new(
        metrics: Vec<StreamWebSearchResultsQueryMetric>,
        category_id: impl Into<String>,
        start_date: chrono::DateTime<chrono::FixedOffset>,
        end_date: chrono::DateTime<chrono::FixedOffset>,
    ) -> Self {
        Self {
            date_interval: None,
            dimensions: None,
            metrics,
            order_by: None,
            pagination: None,
            category_id: category_id.into(),
            start_date,
            end_date,
            filters: None,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct WebSearchResultsQuery {
    /// Date interval for the report. (only used with date dimension)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub date_interval: Option<WebSearchResultsQueryDateInterval>,
    /// Dimensions to group the report by.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dimensions: Option<Vec<WebSearchResultsQueryDimension>>,
    /// Metrics to include. `search_share` is the per-prompt occurrence rate.
    pub metrics: Vec<WebSearchResultsQueryMetric>,
    ///
    /// ```text
    /// Custom ordering of the report results.
    ///
    /// The order is a record of key-value pairs where:
    /// - `key` is the field to order by, which can be a metric or dimension
    /// - `value` is the direction of the order, either `asc` for ascending or `desc` for descending.
    ///
    /// When not specified, the default order is the first metric in the query descending.
    /// ```
    ///             
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_by: Option<std::collections::HashMap<String, WebSearchResultsQueryOrderBy>>,
    /// Pagination settings for the report results.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pagination: Option<Pagination>,
    pub category_id: String,
    /// Start date for the report. Accepts formats: YYYY-MM-DD, YYYY-MM-DD HH:MM, or full ISO timestamp.
    pub start_date: chrono::DateTime<chrono::FixedOffset>,
    /// End date for the report. Accepts formats: YYYY-MM-DD, YYYY-MM-DD HH:MM, or full ISO timestamp.
    pub end_date: chrono::DateTime<chrono::FixedOffset>,
    /// List of filters to apply to the web search results report.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub filters: Option<Vec<WebSearchResultsQueryFilter>>,
}

impl WebSearchResultsQuery {
    /// Creates a new `WebSearchResultsQuery` from its required fields, leaving every optional one unset.
    ///
    /// Pair it with struct-update syntax to set only the optionals you need:
    /// `WebSearchResultsQuery { date_interval: Some(…), ..WebSearchResultsQuery::new(…) }`.
    pub fn new(
        metrics: Vec<WebSearchResultsQueryMetric>,
        category_id: impl Into<String>,
        start_date: chrono::DateTime<chrono::FixedOffset>,
        end_date: chrono::DateTime<chrono::FixedOffset>,
    ) -> Self {
        Self {
            date_interval: None,
            dimensions: None,
            metrics,
            order_by: None,
            pagination: None,
            category_id: category_id.into(),
            start_date,
            end_date,
            filters: None,
        }
    }
}

///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct WebSearchResultsResponse {
    /// Base model for report information.
    pub info: ReportInfo,
    pub data: Vec<WebSearchResultsResult>,
}

///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct WebSearchResultsResult {
    pub metrics: Vec<WebSearchResultsResultMetric>,
    pub dimensions: Vec<Option<WebSearchResultsResultDimension>>,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
#[non_exhaustive]
pub enum WebSearchResultsStreamResponse {
    SseSummaryEventData(WebSearchResultsStreamResponseVariant1),
    Variant2(serde_json::Value),
    /// A variant not recognized by this version of the SDK, preserved verbatim.
    ///
    /// Match the specific variants first. Matching this one directly is an
    /// anti-pattern: that arm silently stops matching once a future SDK
    /// release adds the real variant.
    #[serde(untagged)]
    Unknown(serde_json::Value),
}

///
/// Response-only model, marked `#[non_exhaustive]`: fields may be added
/// additively in future versions without a breaking release.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct WebSearchResultsStreamResponseVariant1 {
    /// The normalized query used to build the stream.
    pub query: serde_json::Value,
    /// Total number of rows available before pagination is applied.
    pub total_rows: i64,
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum SearchQueryFilterOperator {
    #[serde(rename = "is")]
    Is,
    #[serde(rename = "not_is")]
    NotIs,
    #[serde(rename = "in")]
    In,
    #[serde(rename = "not_in")]
    NotIn,
    #[serde(rename = "contains")]
    Contains,
    #[serde(rename = "not_contains")]
    NotContains,
    #[serde(rename = "matches")]
    Matches,
    #[serde(rename = "contains_case_insensitive")]
    ContainsCaseInsensitive,
    #[serde(rename = "not_contains_case_insensitive")]
    NotContainsCaseInsensitive,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl SearchQueryFilterOperator {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Is => "is",
            Self::NotIs => "not_is",
            Self::In => "in",
            Self::NotIn => "not_in",
            Self::Contains => "contains",
            Self::NotContains => "not_contains",
            Self::Matches => "matches",
            Self::ContainsCaseInsensitive => "contains_case_insensitive",
            Self::NotContainsCaseInsensitive => "not_contains_case_insensitive",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for SearchQueryFilterOperator {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for SearchQueryFilterOperator {
    fn from(value: &str) -> Self {
        match value {
            "is" => Self::Is,
            "not_is" => Self::NotIs,
            "in" => Self::In,
            "not_in" => Self::NotIn,
            "contains" => Self::Contains,
            "not_contains" => Self::NotContains,
            "matches" => Self::Matches,
            "contains_case_insensitive" => Self::ContainsCaseInsensitive,
            "not_contains_case_insensitive" => Self::NotContainsCaseInsensitive,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
#[non_exhaustive]
pub enum SearchQueryFilterValue {
    Variant2(Vec<String>),
    Variant1(String),
    /// A variant not recognized by this version of the SDK, preserved verbatim.
    ///
    /// Match the specific variants first. Matching this one directly is an
    /// anti-pattern: that arm silently stops matching once a future SDK
    /// release adds the real variant.
    #[serde(untagged)]
    Unknown(serde_json::Value),
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum StreamWebSearchResultsQueryDateInterval {
    #[serde(rename = "hour")]
    Hour,
    #[serde(rename = "day")]
    Day,
    #[serde(rename = "week")]
    Week,
    #[serde(rename = "month")]
    Month,
    #[serde(rename = "quarter")]
    Quarter,
    #[serde(rename = "year")]
    Year,
    #[serde(rename = "relative_week")]
    RelativeWeek,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl StreamWebSearchResultsQueryDateInterval {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Hour => "hour",
            Self::Day => "day",
            Self::Week => "week",
            Self::Month => "month",
            Self::Quarter => "quarter",
            Self::Year => "year",
            Self::RelativeWeek => "relative_week",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for StreamWebSearchResultsQueryDateInterval {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for StreamWebSearchResultsQueryDateInterval {
    fn from(value: &str) -> Self {
        match value {
            "hour" => Self::Hour,
            "day" => Self::Day,
            "week" => Self::Week,
            "month" => Self::Month,
            "quarter" => Self::Quarter,
            "year" => Self::Year,
            "relative_week" => Self::RelativeWeek,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum StreamWebSearchResultsQueryDimension {
    #[serde(rename = "hostname")]
    Hostname,
    #[serde(rename = "path")]
    Path,
    #[serde(rename = "url")]
    Url,
    #[serde(rename = "root_domain")]
    RootDomain,
    #[serde(rename = "date")]
    Date,
    #[serde(rename = "region")]
    Region,
    #[serde(rename = "topic")]
    Topic,
    #[serde(rename = "topic_id")]
    TopicId,
    #[serde(rename = "model")]
    Model,
    #[serde(rename = "tag")]
    Tag,
    #[serde(rename = "prompt")]
    Prompt,
    #[serde(rename = "prompt_id")]
    PromptId,
    #[serde(rename = "persona")]
    Persona,
    #[serde(rename = "search_query")]
    SearchQuery,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl StreamWebSearchResultsQueryDimension {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Hostname => "hostname",
            Self::Path => "path",
            Self::Url => "url",
            Self::RootDomain => "root_domain",
            Self::Date => "date",
            Self::Region => "region",
            Self::Topic => "topic",
            Self::TopicId => "topic_id",
            Self::Model => "model",
            Self::Tag => "tag",
            Self::Prompt => "prompt",
            Self::PromptId => "prompt_id",
            Self::Persona => "persona",
            Self::SearchQuery => "search_query",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for StreamWebSearchResultsQueryDimension {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for StreamWebSearchResultsQueryDimension {
    fn from(value: &str) -> Self {
        match value {
            "hostname" => Self::Hostname,
            "path" => Self::Path,
            "url" => Self::Url,
            "root_domain" => Self::RootDomain,
            "date" => Self::Date,
            "region" => Self::Region,
            "topic" => Self::Topic,
            "topic_id" => Self::TopicId,
            "model" => Self::Model,
            "tag" => Self::Tag,
            "prompt" => Self::Prompt,
            "prompt_id" => Self::PromptId,
            "persona" => Self::Persona,
            "search_query" => Self::SearchQuery,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum StreamWebSearchResultsQueryMetric {
    #[serde(rename = "count")]
    Count,
    #[serde(rename = "search_share")]
    SearchShare,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl StreamWebSearchResultsQueryMetric {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Count => "count",
            Self::SearchShare => "search_share",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for StreamWebSearchResultsQueryMetric {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for StreamWebSearchResultsQueryMetric {
    fn from(value: &str) -> Self {
        match value {
            "count" => Self::Count,
            "search_share" => Self::SearchShare,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum StreamWebSearchResultsQueryOrderBy {
    #[serde(rename = "asc")]
    Asc,
    #[serde(rename = "desc")]
    Desc,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl StreamWebSearchResultsQueryOrderBy {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Asc => "asc",
            Self::Desc => "desc",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for StreamWebSearchResultsQueryOrderBy {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for StreamWebSearchResultsQueryOrderBy {
    fn from(value: &str) -> Self {
        match value {
            "asc" => Self::Asc,
            "desc" => Self::Desc,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(tag = "field")]
#[non_exhaustive]
pub enum StreamWebSearchResultsQueryFilter {
    #[serde(rename = "hostname")]
    HostnameFilter(HostnameFilter),
    #[serde(rename = "model")]
    ModelIdFilter(ModelIdFilter),
    #[serde(rename = "model_id")]
    ModelIdFilter_(ModelIdFilter),
    #[serde(rename = "path")]
    PathFilter(PathFilter),
    #[serde(rename = "persona_id")]
    PersonaIdFilter(PersonaIdFilter),
    #[serde(rename = "prompt")]
    PromptFilter(PromptFilter),
    #[serde(rename = "prompt_id")]
    PromptIdFilter(PromptIdFilter),
    #[serde(rename = "region")]
    RegionIdFilter(RegionIdFilter),
    #[serde(rename = "region_id")]
    RegionIdFilter_(RegionIdFilter),
    #[serde(rename = "root_domain")]
    RootDomainFilter(RootDomainFilter),
    #[serde(rename = "search_query")]
    SearchQueryFilter(SearchQueryFilter),
    #[serde(rename = "tag")]
    TagIdFilter(TagIdFilter),
    #[serde(rename = "tag_id")]
    TagIdFilter_(TagIdFilter),
    #[serde(rename = "topic")]
    TopicIdFilter(TopicIdFilter),
    #[serde(rename = "topic_id")]
    TopicIdFilter_(TopicIdFilter),
    #[serde(rename = "url")]
    UrlFilter(UrlFilter),
    /// A variant not recognized by this version of the SDK, preserved verbatim.
    ///
    /// Match the specific variants first. Matching this one directly is an
    /// anti-pattern: that arm silently stops matching once a future SDK
    /// release adds the real variant.
    #[serde(untagged)]
    Unknown(serde_json::Value),
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum WebSearchResultsQueryDateInterval {
    #[serde(rename = "hour")]
    Hour,
    #[serde(rename = "day")]
    Day,
    #[serde(rename = "week")]
    Week,
    #[serde(rename = "month")]
    Month,
    #[serde(rename = "quarter")]
    Quarter,
    #[serde(rename = "year")]
    Year,
    #[serde(rename = "relative_week")]
    RelativeWeek,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl WebSearchResultsQueryDateInterval {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Hour => "hour",
            Self::Day => "day",
            Self::Week => "week",
            Self::Month => "month",
            Self::Quarter => "quarter",
            Self::Year => "year",
            Self::RelativeWeek => "relative_week",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for WebSearchResultsQueryDateInterval {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for WebSearchResultsQueryDateInterval {
    fn from(value: &str) -> Self {
        match value {
            "hour" => Self::Hour,
            "day" => Self::Day,
            "week" => Self::Week,
            "month" => Self::Month,
            "quarter" => Self::Quarter,
            "year" => Self::Year,
            "relative_week" => Self::RelativeWeek,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum WebSearchResultsQueryDimension {
    #[serde(rename = "hostname")]
    Hostname,
    #[serde(rename = "path")]
    Path,
    #[serde(rename = "url")]
    Url,
    #[serde(rename = "root_domain")]
    RootDomain,
    #[serde(rename = "date")]
    Date,
    #[serde(rename = "region")]
    Region,
    #[serde(rename = "topic")]
    Topic,
    #[serde(rename = "topic_id")]
    TopicId,
    #[serde(rename = "model")]
    Model,
    #[serde(rename = "tag")]
    Tag,
    #[serde(rename = "prompt")]
    Prompt,
    #[serde(rename = "prompt_id")]
    PromptId,
    #[serde(rename = "persona")]
    Persona,
    #[serde(rename = "search_query")]
    SearchQuery,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl WebSearchResultsQueryDimension {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Hostname => "hostname",
            Self::Path => "path",
            Self::Url => "url",
            Self::RootDomain => "root_domain",
            Self::Date => "date",
            Self::Region => "region",
            Self::Topic => "topic",
            Self::TopicId => "topic_id",
            Self::Model => "model",
            Self::Tag => "tag",
            Self::Prompt => "prompt",
            Self::PromptId => "prompt_id",
            Self::Persona => "persona",
            Self::SearchQuery => "search_query",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for WebSearchResultsQueryDimension {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for WebSearchResultsQueryDimension {
    fn from(value: &str) -> Self {
        match value {
            "hostname" => Self::Hostname,
            "path" => Self::Path,
            "url" => Self::Url,
            "root_domain" => Self::RootDomain,
            "date" => Self::Date,
            "region" => Self::Region,
            "topic" => Self::Topic,
            "topic_id" => Self::TopicId,
            "model" => Self::Model,
            "tag" => Self::Tag,
            "prompt" => Self::Prompt,
            "prompt_id" => Self::PromptId,
            "persona" => Self::Persona,
            "search_query" => Self::SearchQuery,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum WebSearchResultsQueryMetric {
    #[serde(rename = "count")]
    Count,
    #[serde(rename = "search_share")]
    SearchShare,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl WebSearchResultsQueryMetric {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Count => "count",
            Self::SearchShare => "search_share",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for WebSearchResultsQueryMetric {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for WebSearchResultsQueryMetric {
    fn from(value: &str) -> Self {
        match value {
            "count" => Self::Count,
            "search_share" => Self::SearchShare,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

/// Match on `as_str()` (or build one with `From<&str>`) when you need a value
/// this SDK version does not know. Matching `Unknown(_)` directly is an
/// anti-pattern: that arm silently stops matching once a future SDK release
/// adds the real variant.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[non_exhaustive]
pub enum WebSearchResultsQueryOrderBy {
    #[serde(rename = "asc")]
    Asc,
    #[serde(rename = "desc")]
    Desc,
    /// A value not known to this version of the SDK, preserved verbatim.
    #[serde(untagged)]
    Unknown(String),
}

impl WebSearchResultsQueryOrderBy {
    /// The wire value this variant serializes to.
    pub fn as_str(&self) -> &str {
        match self {
            Self::Asc => "asc",
            Self::Desc => "desc",
            Self::Unknown(value) => value,
        }
    }
}

impl std::fmt::Display for WebSearchResultsQueryOrderBy {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str(self.as_str())
    }
}

impl From<&str> for WebSearchResultsQueryOrderBy {
    fn from(value: &str) -> Self {
        match value {
            "asc" => Self::Asc,
            "desc" => Self::Desc,
            other => Self::Unknown(other.to_owned()),
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(tag = "field")]
#[non_exhaustive]
pub enum WebSearchResultsQueryFilter {
    #[serde(rename = "hostname")]
    HostnameFilter(HostnameFilter),
    #[serde(rename = "model")]
    ModelIdFilter(ModelIdFilter),
    #[serde(rename = "model_id")]
    ModelIdFilter_(ModelIdFilter),
    #[serde(rename = "path")]
    PathFilter(PathFilter),
    #[serde(rename = "persona_id")]
    PersonaIdFilter(PersonaIdFilter),
    #[serde(rename = "prompt")]
    PromptFilter(PromptFilter),
    #[serde(rename = "prompt_id")]
    PromptIdFilter(PromptIdFilter),
    #[serde(rename = "region")]
    RegionIdFilter(RegionIdFilter),
    #[serde(rename = "region_id")]
    RegionIdFilter_(RegionIdFilter),
    #[serde(rename = "root_domain")]
    RootDomainFilter(RootDomainFilter),
    #[serde(rename = "search_query")]
    SearchQueryFilter(SearchQueryFilter),
    #[serde(rename = "tag")]
    TagIdFilter(TagIdFilter),
    #[serde(rename = "tag_id")]
    TagIdFilter_(TagIdFilter),
    #[serde(rename = "topic")]
    TopicIdFilter(TopicIdFilter),
    #[serde(rename = "topic_id")]
    TopicIdFilter_(TopicIdFilter),
    #[serde(rename = "url")]
    UrlFilter(UrlFilter),
    /// A variant not recognized by this version of the SDK, preserved verbatim.
    ///
    /// Match the specific variants first. Matching this one directly is an
    /// anti-pattern: that arm silently stops matching once a future SDK
    /// release adds the real variant.
    #[serde(untagged)]
    Unknown(serde_json::Value),
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
#[non_exhaustive]
pub enum WebSearchResultsResultMetric {
    Variant1(i64),
    Variant2(f64),
    Variant3(String),
    /// A variant not recognized by this version of the SDK, preserved verbatim.
    ///
    /// Match the specific variants first. Matching this one directly is an
    /// anti-pattern: that arm silently stops matching once a future SDK
    /// release adds the real variant.
    #[serde(untagged)]
    Unknown(serde_json::Value),
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
#[non_exhaustive]
pub enum WebSearchResultsResultDimension {
    Variant1(String),
    Variant2(String),
    /// A variant not recognized by this version of the SDK, preserved verbatim.
    ///
    /// Match the specific variants first. Matching this one directly is an
    /// anti-pattern: that arm silently stops matching once a future SDK
    /// release adds the real variant.
    #[serde(untagged)]
    Unknown(serde_json::Value),
}
