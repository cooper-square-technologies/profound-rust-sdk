// Code generated by Scalar SDK Generator. DO NOT EDIT.
//! The `generations` resource.

#[derive(Clone)]
pub struct GenerationsResource {
    client: crate::client::ProfoundClient,
}

impl GenerationsResource {
    pub(crate) fn new(client: crate::client::ProfoundClient) -> Self {
        Self { client }
    }

    /// List Project Generations
    pub fn list(&self, category_id: impl Into<String>) -> ListRequestBuilder {
        ListRequestBuilder::new(self.client.clone(), category_id.into())
    }

    /// Get Project Generation Status
    pub fn retrieve(&self, run_id: impl Into<String>, category_id: impl Into<String>) -> RetrieveRequestBuilder {
        RetrieveRequestBuilder::new(self.client.clone(), run_id.into(), category_id.into())
    }
}

/// List Project Generations
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct ListRequestBuilder {
    client: crate::client::ProfoundClient,
    category_id: String,
    status: Option<String>,
    limit: Option<i64>,
    offset: Option<i64>,
    timeout: Option<std::time::Duration>,
    max_retries: Option<u32>,
}

impl ListRequestBuilder {
    fn new(client: crate::client::ProfoundClient, category_id: String) -> Self {
        Self {
            client,
            category_id,
            status: None,
            limit: None,
            offset: None,
            timeout: None,
            max_retries: None,
        }
    }

    /// Comma-separated generation statuses: queued, running, completed, failed.
    pub fn status(mut self, value: impl Into<String>) -> Self {
        self.status = Some(value.into());
        self
    }

    pub fn limit(mut self, value: i64) -> Self {
        self.limit = Some(value);
        self
    }

    pub fn offset(mut self, value: i64) -> Self {
        self.offset = Some(value);
        self
    }

    /// Overrides the client's request deadline for this request only.
    /// It bounds each attempt up to response headers, then the buffered
    /// body read, so total wall time can exceed the value set here. A
    /// streamed body is never deadline-killed; see `ClientBuilder::timeout`.
    pub fn timeout(mut self, timeout: std::time::Duration) -> Self {
        self.timeout = Some(timeout);
        self
    }

    /// Overrides the client's maximum retry attempts for this request only.
    pub fn max_retries(mut self, max_retries: u32) -> Self {
        self.max_retries = Some(max_retries);
        self
    }

    /// Sends the request and returns the decoded response.
    ///
    /// # Errors
    ///
    /// - [`Error::Api`](crate::error::Error::Api) — the server answered with a
    ///   non-success status; the status, headers, request id, and decoded body are
    ///   preserved on the [`ApiError`](crate::error::ApiError).
    /// - [`Error::Transport`](crate::error::Error::Transport) — the request never
    ///   produced a response (connection failure, or the deadline elapsed).
    /// - [`Error::Config`](crate::error::Error::Config) — a parameter value could not
    ///   be rendered into the request.
    /// - [`Error::Serde`](crate::error::Error::Serde) — the response body did not
    ///   match the generated model.
    pub async fn send(self) -> Result<crate::models::ListProjectGenerationsResponse, crate::error::Error> {
        let path = "/v1/projects/generations".to_string();
        let mut query: Vec<(String, String)> = Vec::new();
        let value = &self.category_id;
        query.push(("category_id".to_string(), crate::http::scalar_value(value)));
        if let Some(value) = &self.status {
            query.push(("status".to_string(), crate::http::scalar_value(value)));
        }
        if let Some(value) = &self.limit {
            query.push(("limit".to_string(), crate::http::scalar_value(value)));
        }
        if let Some(value) = &self.offset {
            query.push(("offset".to_string(), crate::http::scalar_value(value)));
        }
        let headers: Vec<(String, String)> = Vec::new();
        let body: Option<&serde_json::Value> = None;
        let overrides = crate::client::RequestOverrides {
            timeout: self.timeout,
            max_retries: self.max_retries,
        };
        self.client
            .send::<crate::models::ListProjectGenerationsResponse, _>(
                http::Method::GET,
                &path,
                &query,
                &headers,
                body,
                true,
                overrides,
            )
            .await
    }
}
/// Get Project Generation Status
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct RetrieveRequestBuilder {
    client: crate::client::ProfoundClient,
    run_id: String,
    category_id: String,
    timeout: Option<std::time::Duration>,
    max_retries: Option<u32>,
}

impl RetrieveRequestBuilder {
    fn new(client: crate::client::ProfoundClient, run_id: String, category_id: String) -> Self {
        Self {
            client,
            run_id,
            category_id,
            timeout: None,
            max_retries: None,
        }
    }

    /// Overrides the client's request deadline for this request only.
    /// It bounds each attempt up to response headers, then the buffered
    /// body read, so total wall time can exceed the value set here. A
    /// streamed body is never deadline-killed; see `ClientBuilder::timeout`.
    pub fn timeout(mut self, timeout: std::time::Duration) -> Self {
        self.timeout = Some(timeout);
        self
    }

    /// Overrides the client's maximum retry attempts for this request only.
    pub fn max_retries(mut self, max_retries: u32) -> Self {
        self.max_retries = Some(max_retries);
        self
    }

    /// Sends the request and returns the decoded response.
    ///
    /// # Errors
    ///
    /// - [`Error::Api`](crate::error::Error::Api) — the server answered with a
    ///   non-success status; the status, headers, request id, and decoded body are
    ///   preserved on the [`ApiError`](crate::error::ApiError).
    /// - [`Error::Transport`](crate::error::Error::Transport) — the request never
    ///   produced a response (connection failure, or the deadline elapsed).
    /// - [`Error::Config`](crate::error::Error::Config) — a parameter value could not
    ///   be rendered into the request.
    /// - [`Error::Serde`](crate::error::Error::Serde) — the response body did not
    ///   match the generated model.
    pub async fn send(self) -> Result<crate::models::ProjectGenerationStatusResponse, crate::error::Error> {
        let path = format!(
            "/v1/projects/generations/{}",
            crate::http::encode_path_param(&self.run_id)
        );
        let mut query: Vec<(String, String)> = Vec::new();
        let value = &self.category_id;
        query.push(("category_id".to_string(), crate::http::scalar_value(value)));
        let headers: Vec<(String, String)> = Vec::new();
        let body: Option<&serde_json::Value> = None;
        let overrides = crate::client::RequestOverrides {
            timeout: self.timeout,
            max_retries: self.max_retries,
        };
        self.client
            .send::<crate::models::ProjectGenerationStatusResponse, _>(
                http::Method::GET,
                &path,
                &query,
                &headers,
                body,
                true,
                overrides,
            )
            .await
    }
}
