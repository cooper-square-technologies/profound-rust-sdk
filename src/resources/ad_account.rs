// Code generated by Scalar SDK Generator. DO NOT EDIT.
//! The `adAccount` resource.

#[derive(Clone)]
pub struct AdAccountResource {
    client: crate::client::ProfoundClient,
}

impl AdAccountResource {
    pub(crate) fn new(client: crate::client::ProfoundClient) -> Self {
        Self { client }
    }

    /// Get Account Insights
    pub fn retrieve_insights(&self) -> RetrieveInsightsRequestBuilder {
        RetrieveInsightsRequestBuilder::new(self.client.clone())
    }
}

/// Get ad account insights for the organization's OpenAI Ads partner brand.
///
/// `aggregation_level=campaign` returns one row per campaign (with `campaign_id`
/// / `campaign_name` and all metrics), so every campaign's insights come back in
/// a single call; `time_granularity=daily` gives per-day rows (e.g. daily spend).
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct RetrieveInsightsRequestBuilder {
    client: crate::client::ProfoundClient,
    organization_id: Option<String>,
    aggregation_level: Option<String>,
    time_granularity: Option<String>,
    time_ranges: Option<Vec<String>>,
    limit: Option<i64>,
    after: Option<String>,
    before: Option<String>,
    timeout: Option<std::time::Duration>,
    max_retries: Option<u32>,
}

impl RetrieveInsightsRequestBuilder {
    fn new(client: crate::client::ProfoundClient) -> Self {
        Self {
            client,
            organization_id: None,
            aggregation_level: None,
            time_granularity: None,
            time_ranges: None,
            limit: None,
            after: None,
            before: None,
            timeout: None,
            max_retries: None,
        }
    }

    /// Organization scope for API keys that can access multiple organizations.
    pub fn organization_id(mut self, value: impl Into<String>) -> Self {
        self.organization_id = Some(value.into());
        self
    }

    /// Row entity for the insights breakdown. `campaign` returns one row per campaign.
    pub fn aggregation_level(mut self, value: impl Into<String>) -> Self {
        self.aggregation_level = Some(value.into());
        self
    }

    /// Time bucket for the rows; `none` or omitted returns totals over the whole range.
    pub fn time_granularity(mut self, value: impl Into<String>) -> Self {
        self.time_granularity = Some(value.into());
        self
    }

    /// Time ranges as JSON objects, e.g. `{"type": "date_range", "since": "2026-07-01", "until": "2026-07-18"}`.
    pub fn time_ranges(mut self, value: Vec<String>) -> Self {
        self.time_ranges = Some(value);
        self
    }

    /// Maximum rows to return.
    pub fn limit(mut self, value: i64) -> Self {
        self.limit = Some(value);
        self
    }

    /// Return items after this ID (forward pagination).
    pub fn after(mut self, value: impl Into<String>) -> Self {
        self.after = Some(value.into());
        self
    }

    /// Return items before this ID (backward pagination).
    pub fn before(mut self, value: impl Into<String>) -> Self {
        self.before = Some(value.into());
        self
    }

    /// Overrides the client's request deadline for this request only.
    /// It bounds each attempt up to response headers, then the buffered
    /// body read, so total wall time can exceed the value set here. A
    /// streamed body is never deadline-killed; see `ClientBuilder::timeout`.
    pub fn timeout(mut self, timeout: std::time::Duration) -> Self {
        self.timeout = Some(timeout);
        self
    }

    /// Overrides the client's maximum retry attempts for this request only.
    pub fn max_retries(mut self, max_retries: u32) -> Self {
        self.max_retries = Some(max_retries);
        self
    }

    /// Sends the request and returns the decoded response.
    ///
    /// # Errors
    ///
    /// - [`Error::Api`](crate::error::Error::Api) — the server answered with a
    ///   non-success status; the status, headers, request id, and decoded body are
    ///   preserved on the [`ApiError`](crate::error::ApiError).
    /// - [`Error::Transport`](crate::error::Error::Transport) — the request never
    ///   produced a response (connection failure, or the deadline elapsed).
    /// - [`Error::Config`](crate::error::Error::Config) — a parameter value could not
    ///   be rendered into the request.
    /// - [`Error::Serde`](crate::error::Error::Serde) — the response body did not
    ///   match the generated model.
    pub async fn send(self) -> Result<crate::models::InsightsResponse, crate::error::Error> {
        let path = "/v1/ads/openai-ads/ad-account/insights".to_string();
        let mut query: Vec<(String, String)> = Vec::new();
        if let Some(value) = &self.organization_id {
            query.push(("organization_id".to_string(), crate::http::scalar_value(value)));
        }
        if let Some(value) = &self.aggregation_level {
            query.push(("aggregation_level".to_string(), crate::http::scalar_value(value)));
        }
        if let Some(value) = &self.time_granularity {
            query.push(("time_granularity".to_string(), crate::http::scalar_value(value)));
        }
        if let Some(values) = &self.time_ranges {
            for item in values {
                query.push(("time_ranges[]".to_string(), crate::http::scalar_value(item)));
            }
        }
        if let Some(value) = &self.limit {
            query.push(("limit".to_string(), crate::http::scalar_value(value)));
        }
        if let Some(value) = &self.after {
            query.push(("after".to_string(), crate::http::scalar_value(value)));
        }
        if let Some(value) = &self.before {
            query.push(("before".to_string(), crate::http::scalar_value(value)));
        }
        let headers: Vec<(String, String)> = Vec::new();
        let body: Option<&serde_json::Value> = None;
        let overrides = crate::client::RequestOverrides {
            timeout: self.timeout,
            max_retries: self.max_retries,
        };
        self.client
            .send::<crate::models::InsightsResponse, _>(
                http::Method::GET,
                &path,
                &query,
                &headers,
                body,
                true,
                overrides,
            )
            .await
    }
}
