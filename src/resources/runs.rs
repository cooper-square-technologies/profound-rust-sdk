// Code generated by Scalar SDK Generator. DO NOT EDIT.
//! The `runs` resource.

#[derive(Clone)]
pub struct RunsResource {
    client: crate::client::ProfoundClient,
}

impl RunsResource {
    pub(crate) fn new(client: crate::client::ProfoundClient) -> Self {
        Self { client }
    }

    /// Run an agent
    pub fn create(&self, agent_id: impl Into<String>) -> CreateRequestBuilder {
        CreateRequestBuilder::new(self.client.clone(), agent_id.into())
    }

    /// Get an agent run
    pub fn retrieve(&self, agent_id: impl Into<String>, run_id: impl Into<String>) -> RetrieveRequestBuilder {
        RetrieveRequestBuilder::new(self.client.clone(), agent_id.into(), run_id.into())
    }
}

/// Start a new run for an agent.
///
/// Runs always execute the agent's live published version, so the agent must be
/// published first with `POST /v1/agents/{agent_id}/publish`. Unpublished drafts
/// cannot be run.
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct CreateRequestBuilder {
    client: crate::client::ProfoundClient,
    agent_id: String,
    body: Option<crate::models::RunAgentRequest>,
    idempotency_key: Option<String>,
    timeout: Option<std::time::Duration>,
    max_retries: Option<u32>,
}

impl CreateRequestBuilder {
    fn new(client: crate::client::ProfoundClient, agent_id: String) -> Self {
        Self {
            client,
            agent_id,
            body: None,
            idempotency_key: None,
            timeout: None,
            max_retries: None,
        }
    }

    pub fn body(mut self, value: crate::models::RunAgentRequest) -> Self {
        self.body = Some(value);
        self
    }

    /// Sets an idempotency key so this request can be safely retried.
    pub fn idempotency_key(mut self, value: impl Into<String>) -> Self {
        self.idempotency_key = Some(value.into());
        self
    }

    /// Overrides the client's request deadline for this request only.
    /// It bounds each attempt up to response headers, then the buffered
    /// body read, so total wall time can exceed the value set here. A
    /// streamed body is never deadline-killed; see `ClientBuilder::timeout`.
    pub fn timeout(mut self, timeout: std::time::Duration) -> Self {
        self.timeout = Some(timeout);
        self
    }

    /// Overrides the client's maximum retry attempts for this request only.
    pub fn max_retries(mut self, max_retries: u32) -> Self {
        self.max_retries = Some(max_retries);
        self
    }

    /// Sends the request and returns the decoded response.
    ///
    /// # Errors
    ///
    /// - [`Error::Api`](crate::error::Error::Api) — the server answered with a
    ///   non-success status; the status, headers, request id, and decoded body are
    ///   preserved on the [`ApiError`](crate::error::ApiError).
    /// - [`Error::Transport`](crate::error::Error::Transport) — the request never
    ///   produced a response (connection failure, or the deadline elapsed).
    /// - [`Error::Config`](crate::error::Error::Config) — a parameter value could not
    ///   be rendered into the request.
    /// - [`Error::Serde`](crate::error::Error::Serde) — the response body did not
    ///   match the generated model.
    pub async fn send(self) -> Result<crate::models::AcceptedAgentRun, crate::error::Error> {
        let path = format!("/v1/agents/{}/runs", crate::http::encode_path_param(&self.agent_id));
        let query: Vec<(String, String)> = Vec::new();
        let mut headers: Vec<(String, String)> = Vec::new();
        if let Some(key) = self.idempotency_key {
            headers.push(("Idempotency-Key".to_string(), key));
        }
        let body = self.body.as_ref();
        let overrides = crate::client::RequestOverrides {
            timeout: self.timeout,
            max_retries: self.max_retries,
        };
        self.client
            .send::<crate::models::AcceptedAgentRun, _>(
                http::Method::POST,
                &path,
                &query,
                &headers,
                body,
                true,
                overrides,
            )
            .await
    }
}
/// Retrieve the current status and result details for an agent run.
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct RetrieveRequestBuilder {
    client: crate::client::ProfoundClient,
    agent_id: String,
    run_id: String,
    verbose: Option<bool>,
    timeout: Option<std::time::Duration>,
    max_retries: Option<u32>,
}

impl RetrieveRequestBuilder {
    fn new(client: crate::client::ProfoundClient, agent_id: String, run_id: String) -> Self {
        Self {
            client,
            agent_id,
            run_id,
            verbose: None,
            timeout: None,
            max_retries: None,
        }
    }

    /// Include each step's raw `outputs` payload in the execution trace.
    pub fn verbose(mut self, value: bool) -> Self {
        self.verbose = Some(value);
        self
    }

    /// Overrides the client's request deadline for this request only.
    /// It bounds each attempt up to response headers, then the buffered
    /// body read, so total wall time can exceed the value set here. A
    /// streamed body is never deadline-killed; see `ClientBuilder::timeout`.
    pub fn timeout(mut self, timeout: std::time::Duration) -> Self {
        self.timeout = Some(timeout);
        self
    }

    /// Overrides the client's maximum retry attempts for this request only.
    pub fn max_retries(mut self, max_retries: u32) -> Self {
        self.max_retries = Some(max_retries);
        self
    }

    /// Sends the request and returns the decoded response.
    ///
    /// # Errors
    ///
    /// - [`Error::Api`](crate::error::Error::Api) — the server answered with a
    ///   non-success status; the status, headers, request id, and decoded body are
    ///   preserved on the [`ApiError`](crate::error::ApiError).
    /// - [`Error::Transport`](crate::error::Error::Transport) — the request never
    ///   produced a response (connection failure, or the deadline elapsed).
    /// - [`Error::Config`](crate::error::Error::Config) — a parameter value could not
    ///   be rendered into the request.
    /// - [`Error::Serde`](crate::error::Error::Serde) — the response body did not
    ///   match the generated model.
    pub async fn send(self) -> Result<crate::models::AgentRun, crate::error::Error> {
        let path = format!(
            "/v1/agents/{}/runs/{}",
            crate::http::encode_path_param(&self.agent_id),
            crate::http::encode_path_param(&self.run_id)
        );
        let mut query: Vec<(String, String)> = Vec::new();
        if let Some(value) = &self.verbose {
            query.push(("verbose".to_string(), crate::http::scalar_value(value)));
        }
        let headers: Vec<(String, String)> = Vec::new();
        let body: Option<&serde_json::Value> = None;
        let overrides = crate::client::RequestOverrides {
            timeout: self.timeout,
            max_retries: self.max_retries,
        };
        self.client
            .send::<crate::models::AgentRun, _>(http::Method::GET, &path, &query, &headers, body, true, overrides)
            .await
    }
}
