// Code generated by Scalar SDK Generator. DO NOT EDIT.
//! The `agents` resource.

#[derive(Clone)]
pub struct AgentsResource {
    client: crate::client::ProfoundClient,
}

impl AgentsResource {
    pub(crate) fn new(client: crate::client::ProfoundClient) -> Self {
        Self { client }
    }

    /// List agents
    pub fn list(&self) -> ListRequestBuilder {
        ListRequestBuilder::new(self.client.clone())
    }

    /// Get an agent
    pub fn retrieve(&self, agent_id: impl Into<String>) -> RetrieveRequestBuilder {
        RetrieveRequestBuilder::new(self.client.clone(), agent_id.into())
    }

    /// Create an agent
    pub fn create(&self, body: crate::models::CreateAgentRequest) -> CreateRequestBuilder {
        CreateRequestBuilder::new(self.client.clone(), body)
    }

    /// Publish an agent
    pub fn publish(&self, agent_id: impl Into<String>) -> PublishRequestBuilder {
        PublishRequestBuilder::new(self.client.clone(), agent_id.into())
    }

    /// Update an agent
    pub fn update(&self, agent_id: impl Into<String>, body: crate::models::UpdateAgentRequest) -> UpdateRequestBuilder {
        UpdateRequestBuilder::new(self.client.clone(), agent_id.into(), body)
    }

    /// Get an agent's graph
    pub fn retrieve_graph(&self, agent_id: impl Into<String>) -> RetrieveGraphRequestBuilder {
        RetrieveGraphRequestBuilder::new(self.client.clone(), agent_id.into())
    }

    pub fn runs(&self) -> crate::resources::runs::RunsResource {
        crate::resources::runs::RunsResource::new(self.client.clone())
    }

    pub fn node_types(&self) -> crate::resources::node_types::NodeTypesResource {
        crate::resources::node_types::NodeTypesResource::new(self.client.clone())
    }
}

/// List agents available to your organization.
///
/// Agent status reflects whether an agent has ever been published. `published`
/// agents have a live published version. `draft` agents have not been
/// published yet.
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct ListRequestBuilder {
    client: crate::client::ProfoundClient,
    statuses: Option<Vec<crate::models::ListAgentsStatusFilter>>,
    limit: Option<i64>,
    next_cursor: Option<String>,
    timeout: Option<std::time::Duration>,
    max_retries: Option<u32>,
}

impl ListRequestBuilder {
    fn new(client: crate::client::ProfoundClient) -> Self {
        Self {
            client,
            statuses: None,
            limit: None,
            next_cursor: None,
            timeout: None,
            max_retries: None,
        }
    }

    /// Optional status filter. Use `published` to list agents that have a live published version, or `draft` to list agents that have not been published yet. Defaults to `published`.
    pub fn statuses(mut self, value: Vec<crate::models::ListAgentsStatusFilter>) -> Self {
        self.statuses = Some(value);
        self
    }

    pub fn limit(mut self, value: i64) -> Self {
        self.limit = Some(value);
        self
    }

    pub fn next_cursor(mut self, value: impl Into<String>) -> Self {
        self.next_cursor = Some(value.into());
        self
    }

    /// Overrides the client's request deadline for this request only.
    /// It bounds each attempt up to response headers, then the buffered
    /// body read, so total wall time can exceed the value set here. A
    /// streamed body is never deadline-killed; see `ClientBuilder::timeout`.
    pub fn timeout(mut self, timeout: std::time::Duration) -> Self {
        self.timeout = Some(timeout);
        self
    }

    /// Overrides the client's maximum retry attempts for this request only.
    pub fn max_retries(mut self, max_retries: u32) -> Self {
        self.max_retries = Some(max_retries);
        self
    }

    /// Sends the request and returns the decoded response.
    ///
    /// # Errors
    ///
    /// - [`Error::Api`](crate::error::Error::Api) — the server answered with a
    ///   non-success status; the status, headers, request id, and decoded body are
    ///   preserved on the [`ApiError`](crate::error::ApiError).
    /// - [`Error::Transport`](crate::error::Error::Transport) — the request never
    ///   produced a response (connection failure, or the deadline elapsed).
    /// - [`Error::Config`](crate::error::Error::Config) — a parameter value could not
    ///   be rendered into the request.
    /// - [`Error::Serde`](crate::error::Error::Serde) — the response body did not
    ///   match the generated model.
    pub async fn send(self) -> Result<crate::models::ListAgentsResponse, crate::error::Error> {
        let path = "/v1/agents".to_string();
        let mut query: Vec<(String, String)> = Vec::new();
        if let Some(values) = &self.statuses {
            for item in values {
                query.push(("statuses".to_string(), crate::http::scalar_value(item)));
            }
        }
        if let Some(value) = &self.limit {
            query.push(("limit".to_string(), crate::http::scalar_value(value)));
        }
        if let Some(value) = &self.next_cursor {
            query.push(("next_cursor".to_string(), crate::http::scalar_value(value)));
        }
        let headers: Vec<(String, String)> = Vec::new();
        let body: Option<&serde_json::Value> = None;
        let overrides = crate::client::RequestOverrides {
            timeout: self.timeout,
            max_retries: self.max_retries,
        };
        self.client
            .send::<crate::models::ListAgentsResponse, _>(
                http::Method::GET,
                &path,
                &query,
                &headers,
                body,
                true,
                overrides,
            )
            .await
    }
}
/// Retrieve an agent and its schema details.
///
/// Agents can have both a live published version and a draft version with newer
/// unpublished changes. Use the `version` parameter to choose which state to return.
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct RetrieveRequestBuilder {
    client: crate::client::ProfoundClient,
    agent_id: String,
    version: Option<crate::models::AgentVersion>,
    timeout: Option<std::time::Duration>,
    max_retries: Option<u32>,
}

impl RetrieveRequestBuilder {
    fn new(client: crate::client::ProfoundClient, agent_id: String) -> Self {
        Self {
            client,
            agent_id,
            version: None,
            timeout: None,
            max_retries: None,
        }
    }

    /// Version of the agent to retrieve. Use `published` for the live version, or `draft` for the latest unpublished changes for the same agent. Defaults to `published`.
    pub fn version(mut self, value: crate::models::AgentVersion) -> Self {
        self.version = Some(value);
        self
    }

    /// Overrides the client's request deadline for this request only.
    /// It bounds each attempt up to response headers, then the buffered
    /// body read, so total wall time can exceed the value set here. A
    /// streamed body is never deadline-killed; see `ClientBuilder::timeout`.
    pub fn timeout(mut self, timeout: std::time::Duration) -> Self {
        self.timeout = Some(timeout);
        self
    }

    /// Overrides the client's maximum retry attempts for this request only.
    pub fn max_retries(mut self, max_retries: u32) -> Self {
        self.max_retries = Some(max_retries);
        self
    }

    /// Sends the request and returns the decoded response.
    ///
    /// # Errors
    ///
    /// - [`Error::Api`](crate::error::Error::Api) — the server answered with a
    ///   non-success status; the status, headers, request id, and decoded body are
    ///   preserved on the [`ApiError`](crate::error::ApiError).
    /// - [`Error::Transport`](crate::error::Error::Transport) — the request never
    ///   produced a response (connection failure, or the deadline elapsed).
    /// - [`Error::Config`](crate::error::Error::Config) — a parameter value could not
    ///   be rendered into the request.
    /// - [`Error::Serde`](crate::error::Error::Serde) — the response body did not
    ///   match the generated model.
    pub async fn send(self) -> Result<crate::models::AgentDetail, crate::error::Error> {
        let path = format!("/v1/agents/{}", crate::http::encode_path_param(&self.agent_id));
        let mut query: Vec<(String, String)> = Vec::new();
        if let Some(value) = &self.version {
            query.push(("version".to_string(), crate::http::scalar_value(value)));
        }
        let headers: Vec<(String, String)> = Vec::new();
        let body: Option<&serde_json::Value> = None;
        let overrides = crate::client::RequestOverrides {
            timeout: self.timeout,
            max_retries: self.max_retries,
        };
        self.client
            .send::<crate::models::AgentDetail, _>(http::Method::GET, &path, &query, &headers, body, true, overrides)
            .await
    }
}
/// Create a new draft agent owned by the given organization.
///
/// `organization_id` is required and you must be a member of it. The agent is created
/// as a `draft`; publish it with `POST /v1/agents/{agent_id}/publish` once its graph
/// is ready.
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct CreateRequestBuilder {
    client: crate::client::ProfoundClient,
    body: crate::models::CreateAgentRequest,
    idempotency_key: Option<String>,
    timeout: Option<std::time::Duration>,
    max_retries: Option<u32>,
}

impl CreateRequestBuilder {
    fn new(client: crate::client::ProfoundClient, body: crate::models::CreateAgentRequest) -> Self {
        Self {
            client,
            body,
            idempotency_key: None,
            timeout: None,
            max_retries: None,
        }
    }

    /// Sets an idempotency key so this request can be safely retried.
    pub fn idempotency_key(mut self, value: impl Into<String>) -> Self {
        self.idempotency_key = Some(value.into());
        self
    }

    /// Overrides the client's request deadline for this request only.
    /// It bounds each attempt up to response headers, then the buffered
    /// body read, so total wall time can exceed the value set here. A
    /// streamed body is never deadline-killed; see `ClientBuilder::timeout`.
    pub fn timeout(mut self, timeout: std::time::Duration) -> Self {
        self.timeout = Some(timeout);
        self
    }

    /// Overrides the client's maximum retry attempts for this request only.
    pub fn max_retries(mut self, max_retries: u32) -> Self {
        self.max_retries = Some(max_retries);
        self
    }

    /// Sends the request and returns the decoded response.
    ///
    /// # Errors
    ///
    /// - [`Error::Api`](crate::error::Error::Api) — the server answered with a
    ///   non-success status; the status, headers, request id, and decoded body are
    ///   preserved on the [`ApiError`](crate::error::ApiError).
    /// - [`Error::Transport`](crate::error::Error::Transport) — the request never
    ///   produced a response (connection failure, or the deadline elapsed).
    /// - [`Error::Config`](crate::error::Error::Config) — a parameter value could not
    ///   be rendered into the request.
    /// - [`Error::Serde`](crate::error::Error::Serde) — the response body did not
    ///   match the generated model.
    pub async fn send(self) -> Result<crate::models::Agent, crate::error::Error> {
        let path = "/v1/agents".to_string();
        let query: Vec<(String, String)> = Vec::new();
        let mut headers: Vec<(String, String)> = Vec::new();
        if let Some(key) = self.idempotency_key {
            headers.push(("Idempotency-Key".to_string(), key));
        }
        let body = Some(&self.body);
        let overrides = crate::client::RequestOverrides {
            timeout: self.timeout,
            max_retries: self.max_retries,
        };
        self.client
            .send::<crate::models::Agent, _>(http::Method::POST, &path, &query, &headers, body, true, overrides)
            .await
    }
}
/// Publish an agent's latest draft as its live published version.
///
/// You must be a member of the agent's organization. Publishing promotes the current
/// draft graph to a new published version. A draft that cannot produce its declared
/// input/output contract is rejected with `422` and is not published.
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct PublishRequestBuilder {
    client: crate::client::ProfoundClient,
    agent_id: String,
    idempotency_key: Option<String>,
    timeout: Option<std::time::Duration>,
    max_retries: Option<u32>,
}

impl PublishRequestBuilder {
    fn new(client: crate::client::ProfoundClient, agent_id: String) -> Self {
        Self {
            client,
            agent_id,
            idempotency_key: None,
            timeout: None,
            max_retries: None,
        }
    }

    /// Sets an idempotency key so this request can be safely retried.
    pub fn idempotency_key(mut self, value: impl Into<String>) -> Self {
        self.idempotency_key = Some(value.into());
        self
    }

    /// Overrides the client's request deadline for this request only.
    /// It bounds each attempt up to response headers, then the buffered
    /// body read, so total wall time can exceed the value set here. A
    /// streamed body is never deadline-killed; see `ClientBuilder::timeout`.
    pub fn timeout(mut self, timeout: std::time::Duration) -> Self {
        self.timeout = Some(timeout);
        self
    }

    /// Overrides the client's maximum retry attempts for this request only.
    pub fn max_retries(mut self, max_retries: u32) -> Self {
        self.max_retries = Some(max_retries);
        self
    }

    /// Sends the request and returns the decoded response.
    ///
    /// # Errors
    ///
    /// - [`Error::Api`](crate::error::Error::Api) — the server answered with a
    ///   non-success status; the status, headers, request id, and decoded body are
    ///   preserved on the [`ApiError`](crate::error::ApiError).
    /// - [`Error::Transport`](crate::error::Error::Transport) — the request never
    ///   produced a response (connection failure, or the deadline elapsed).
    /// - [`Error::Config`](crate::error::Error::Config) — a parameter value could not
    ///   be rendered into the request.
    /// - [`Error::Serde`](crate::error::Error::Serde) — the response body did not
    ///   match the generated model.
    pub async fn send(self) -> Result<crate::models::Agent, crate::error::Error> {
        let path = format!("/v1/agents/{}/publish", crate::http::encode_path_param(&self.agent_id));
        let query: Vec<(String, String)> = Vec::new();
        let mut headers: Vec<(String, String)> = Vec::new();
        if let Some(key) = self.idempotency_key {
            headers.push(("Idempotency-Key".to_string(), key));
        }
        let body: Option<&serde_json::Value> = None;
        let overrides = crate::client::RequestOverrides {
            timeout: self.timeout,
            max_retries: self.max_retries,
        };
        self.client
            .send::<crate::models::Agent, _>(http::Method::POST, &path, &query, &headers, body, true, overrides)
            .await
    }
}
/// Update an agent's draft graph in place.
///
/// You must be a member of the agent's organization. The agent's draft is replaced with the
/// supplied graph and re-validated, so you can iterate one draft — create, then update per
/// fix — instead of creating a new agent on every change. The response carries the updated
/// `validation`; publish with `POST /v1/agents/{agent_id}/publish` once `validation.valid`.
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct UpdateRequestBuilder {
    client: crate::client::ProfoundClient,
    agent_id: String,
    body: crate::models::UpdateAgentRequest,
    idempotency_key: Option<String>,
    timeout: Option<std::time::Duration>,
    max_retries: Option<u32>,
}

impl UpdateRequestBuilder {
    fn new(client: crate::client::ProfoundClient, agent_id: String, body: crate::models::UpdateAgentRequest) -> Self {
        Self {
            client,
            agent_id,
            body,
            idempotency_key: None,
            timeout: None,
            max_retries: None,
        }
    }

    /// Sets an idempotency key so this request can be safely retried.
    pub fn idempotency_key(mut self, value: impl Into<String>) -> Self {
        self.idempotency_key = Some(value.into());
        self
    }

    /// Overrides the client's request deadline for this request only.
    /// It bounds each attempt up to response headers, then the buffered
    /// body read, so total wall time can exceed the value set here. A
    /// streamed body is never deadline-killed; see `ClientBuilder::timeout`.
    pub fn timeout(mut self, timeout: std::time::Duration) -> Self {
        self.timeout = Some(timeout);
        self
    }

    /// Overrides the client's maximum retry attempts for this request only.
    pub fn max_retries(mut self, max_retries: u32) -> Self {
        self.max_retries = Some(max_retries);
        self
    }

    /// Sends the request and returns the decoded response.
    ///
    /// # Errors
    ///
    /// - [`Error::Api`](crate::error::Error::Api) — the server answered with a
    ///   non-success status; the status, headers, request id, and decoded body are
    ///   preserved on the [`ApiError`](crate::error::ApiError).
    /// - [`Error::Transport`](crate::error::Error::Transport) — the request never
    ///   produced a response (connection failure, or the deadline elapsed).
    /// - [`Error::Config`](crate::error::Error::Config) — a parameter value could not
    ///   be rendered into the request.
    /// - [`Error::Serde`](crate::error::Error::Serde) — the response body did not
    ///   match the generated model.
    pub async fn send(self) -> Result<crate::models::AgentDetail, crate::error::Error> {
        let path = format!("/v1/agents/{}", crate::http::encode_path_param(&self.agent_id));
        let query: Vec<(String, String)> = Vec::new();
        let mut headers: Vec<(String, String)> = Vec::new();
        if let Some(key) = self.idempotency_key {
            headers.push(("Idempotency-Key".to_string(), key));
        }
        let body = Some(&self.body);
        let overrides = crate::client::RequestOverrides {
            timeout: self.timeout,
            max_retries: self.max_retries,
        };
        self.client
            .send::<crate::models::AgentDetail, _>(http::Method::PATCH, &path, &query, &headers, body, true, overrides)
            .await
    }
}
/// Retrieve an agent's full workflow graph (`{nodes, edges}`).
///
/// The graph is returned verbatim in the canonical dialect — the same shape `POST /v1/agents`
/// and `PATCH /v1/agents/{agent_id}` accept — so a known-good agent can be read back, copied,
/// and edited. Tool-backed nodes appear in their lowered `tool` form rather than the friendly
/// v1 node types. A `draft` is visible only to its creator; the `published` version is visible
/// across its organization.
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct RetrieveGraphRequestBuilder {
    client: crate::client::ProfoundClient,
    agent_id: String,
    version: Option<crate::models::AgentVersion>,
    timeout: Option<std::time::Duration>,
    max_retries: Option<u32>,
}

impl RetrieveGraphRequestBuilder {
    fn new(client: crate::client::ProfoundClient, agent_id: String) -> Self {
        Self {
            client,
            agent_id,
            version: None,
            timeout: None,
            max_retries: None,
        }
    }

    /// Version of the agent whose graph to retrieve. Use `published` for the live version, or `draft` for the latest unpublished changes. Defaults to `published`.
    pub fn version(mut self, value: crate::models::AgentVersion) -> Self {
        self.version = Some(value);
        self
    }

    /// Overrides the client's request deadline for this request only.
    /// It bounds each attempt up to response headers, then the buffered
    /// body read, so total wall time can exceed the value set here. A
    /// streamed body is never deadline-killed; see `ClientBuilder::timeout`.
    pub fn timeout(mut self, timeout: std::time::Duration) -> Self {
        self.timeout = Some(timeout);
        self
    }

    /// Overrides the client's maximum retry attempts for this request only.
    pub fn max_retries(mut self, max_retries: u32) -> Self {
        self.max_retries = Some(max_retries);
        self
    }

    /// Sends the request and returns the decoded response.
    ///
    /// # Errors
    ///
    /// - [`Error::Api`](crate::error::Error::Api) — the server answered with a
    ///   non-success status; the status, headers, request id, and decoded body are
    ///   preserved on the [`ApiError`](crate::error::ApiError).
    /// - [`Error::Transport`](crate::error::Error::Transport) — the request never
    ///   produced a response (connection failure, or the deadline elapsed).
    /// - [`Error::Config`](crate::error::Error::Config) — a parameter value could not
    ///   be rendered into the request.
    /// - [`Error::Serde`](crate::error::Error::Serde) — the response body did not
    ///   match the generated model.
    pub async fn send(self) -> Result<crate::models::AgentGraph, crate::error::Error> {
        let path = format!("/v1/agents/{}/graph", crate::http::encode_path_param(&self.agent_id));
        let mut query: Vec<(String, String)> = Vec::new();
        if let Some(value) = &self.version {
            query.push(("version".to_string(), crate::http::scalar_value(value)));
        }
        let headers: Vec<(String, String)> = Vec::new();
        let body: Option<&serde_json::Value> = None;
        let overrides = crate::client::RequestOverrides {
            timeout: self.timeout,
            max_retries: self.max_retries,
        };
        self.client
            .send::<crate::models::AgentGraph, _>(http::Method::GET, &path, &query, &headers, body, true, overrides)
            .await
    }
}
