// Code generated by Scalar SDK Generator. DO NOT EDIT.
//! The `tasks` resource.

#[derive(Clone)]
pub struct TasksResource {
    client: crate::client::ProfoundClient,
}

impl TasksResource {
    pub(crate) fn new(client: crate::client::ProfoundClient) -> Self {
        Self { client }
    }

    /// List Project Tasks
    pub fn list(&self, project_id: impl Into<String>, category_id: impl Into<String>) -> ListRequestBuilder {
        ListRequestBuilder::new(self.client.clone(), project_id.into(), category_id.into())
    }

    /// Create Project Task
    pub fn create(
        &self,
        project_id: impl Into<String>,
        category_id: impl Into<String>,
        body: crate::models::CreateProjectTaskRequest,
    ) -> CreateRequestBuilder {
        CreateRequestBuilder::new(self.client.clone(), project_id.into(), category_id.into(), body)
    }

    /// Get Project Task
    pub fn retrieve(
        &self,
        project_id: impl Into<String>,
        task_id: impl Into<String>,
        category_id: impl Into<String>,
    ) -> RetrieveRequestBuilder {
        RetrieveRequestBuilder::new(
            self.client.clone(),
            project_id.into(),
            task_id.into(),
            category_id.into(),
        )
    }

    /// Update Project Task
    pub fn update(
        &self,
        project_id: impl Into<String>,
        task_id: impl Into<String>,
        category_id: impl Into<String>,
        body: crate::models::UpdateProjectTaskRequest,
    ) -> UpdateRequestBuilder {
        UpdateRequestBuilder::new(
            self.client.clone(),
            project_id.into(),
            task_id.into(),
            category_id.into(),
            body,
        )
    }

    /// Delete Project Task
    pub fn delete(
        &self,
        project_id: impl Into<String>,
        task_id: impl Into<String>,
        category_id: impl Into<String>,
    ) -> DeleteRequestBuilder {
        DeleteRequestBuilder::new(
            self.client.clone(),
            project_id.into(),
            task_id.into(),
            category_id.into(),
        )
    }

    /// Update Project Task Status
    pub fn update_status(
        &self,
        project_id: impl Into<String>,
        task_id: impl Into<String>,
        category_id: impl Into<String>,
        body: crate::models::UpdateProjectTaskStatusRequest,
    ) -> UpdateStatusRequestBuilder {
        UpdateStatusRequestBuilder::new(
            self.client.clone(),
            project_id.into(),
            task_id.into(),
            category_id.into(),
            body,
        )
    }
}

/// List Project Tasks
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct ListRequestBuilder {
    client: crate::client::ProfoundClient,
    project_id: String,
    category_id: String,
    timeout: Option<std::time::Duration>,
    max_retries: Option<u32>,
}

impl ListRequestBuilder {
    fn new(client: crate::client::ProfoundClient, project_id: String, category_id: String) -> Self {
        Self {
            client,
            project_id,
            category_id,
            timeout: None,
            max_retries: None,
        }
    }

    /// Overrides the client's request deadline for this request only.
    /// It bounds each attempt up to response headers, then the buffered
    /// body read, so total wall time can exceed the value set here. A
    /// streamed body is never deadline-killed; see `ClientBuilder::timeout`.
    pub fn timeout(mut self, timeout: std::time::Duration) -> Self {
        self.timeout = Some(timeout);
        self
    }

    /// Overrides the client's maximum retry attempts for this request only.
    pub fn max_retries(mut self, max_retries: u32) -> Self {
        self.max_retries = Some(max_retries);
        self
    }

    /// Sends the request and returns the decoded response.
    ///
    /// # Errors
    ///
    /// - [`Error::Api`](crate::error::Error::Api) — the server answered with a
    ///   non-success status; the status, headers, request id, and decoded body are
    ///   preserved on the [`ApiError`](crate::error::ApiError).
    /// - [`Error::Transport`](crate::error::Error::Transport) — the request never
    ///   produced a response (connection failure, or the deadline elapsed).
    /// - [`Error::Config`](crate::error::Error::Config) — a parameter value could not
    ///   be rendered into the request.
    /// - [`Error::Serde`](crate::error::Error::Serde) — the response body did not
    ///   match the generated model.
    pub async fn send(self) -> Result<crate::models::ListProjectTasksResponse, crate::error::Error> {
        let path = format!(
            "/v1/projects/{}/tasks",
            crate::http::encode_path_param(&self.project_id)
        );
        let mut query: Vec<(String, String)> = Vec::new();
        let value = &self.category_id;
        query.push(("category_id".to_string(), crate::http::scalar_value(value)));
        let headers: Vec<(String, String)> = Vec::new();
        let body: Option<&serde_json::Value> = None;
        let overrides = crate::client::RequestOverrides {
            timeout: self.timeout,
            max_retries: self.max_retries,
        };
        self.client
            .send::<crate::models::ListProjectTasksResponse, _>(
                http::Method::GET,
                &path,
                &query,
                &headers,
                body,
                true,
                overrides,
            )
            .await
    }
}
/// Create Project Task
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct CreateRequestBuilder {
    client: crate::client::ProfoundClient,
    project_id: String,
    category_id: String,
    body: crate::models::CreateProjectTaskRequest,
    idempotency_key: Option<String>,
    timeout: Option<std::time::Duration>,
    max_retries: Option<u32>,
}

impl CreateRequestBuilder {
    fn new(
        client: crate::client::ProfoundClient,
        project_id: String,
        category_id: String,
        body: crate::models::CreateProjectTaskRequest,
    ) -> Self {
        Self {
            client,
            project_id,
            category_id,
            body,
            idempotency_key: None,
            timeout: None,
            max_retries: None,
        }
    }

    /// Sets an idempotency key so this request can be safely retried.
    pub fn idempotency_key(mut self, value: impl Into<String>) -> Self {
        self.idempotency_key = Some(value.into());
        self
    }

    /// Overrides the client's request deadline for this request only.
    /// It bounds each attempt up to response headers, then the buffered
    /// body read, so total wall time can exceed the value set here. A
    /// streamed body is never deadline-killed; see `ClientBuilder::timeout`.
    pub fn timeout(mut self, timeout: std::time::Duration) -> Self {
        self.timeout = Some(timeout);
        self
    }

    /// Overrides the client's maximum retry attempts for this request only.
    pub fn max_retries(mut self, max_retries: u32) -> Self {
        self.max_retries = Some(max_retries);
        self
    }

    /// Sends the request and returns the decoded response.
    ///
    /// # Errors
    ///
    /// - [`Error::Api`](crate::error::Error::Api) — the server answered with a
    ///   non-success status; the status, headers, request id, and decoded body are
    ///   preserved on the [`ApiError`](crate::error::ApiError).
    /// - [`Error::Transport`](crate::error::Error::Transport) — the request never
    ///   produced a response (connection failure, or the deadline elapsed).
    /// - [`Error::Config`](crate::error::Error::Config) — a parameter value could not
    ///   be rendered into the request.
    /// - [`Error::Serde`](crate::error::Error::Serde) — the response body did not
    ///   match the generated model.
    pub async fn send(self) -> Result<crate::models::ProjectTaskResponse, crate::error::Error> {
        let path = format!(
            "/v1/projects/{}/tasks",
            crate::http::encode_path_param(&self.project_id)
        );
        let mut query: Vec<(String, String)> = Vec::new();
        let value = &self.category_id;
        query.push(("category_id".to_string(), crate::http::scalar_value(value)));
        let mut headers: Vec<(String, String)> = Vec::new();
        if let Some(key) = self.idempotency_key {
            headers.push(("Idempotency-Key".to_string(), key));
        }
        let body = Some(&self.body);
        let overrides = crate::client::RequestOverrides {
            timeout: self.timeout,
            max_retries: self.max_retries,
        };
        self.client
            .send::<crate::models::ProjectTaskResponse, _>(
                http::Method::POST,
                &path,
                &query,
                &headers,
                body,
                true,
                overrides,
            )
            .await
    }
}
/// Get Project Task
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct RetrieveRequestBuilder {
    client: crate::client::ProfoundClient,
    project_id: String,
    task_id: String,
    category_id: String,
    timeout: Option<std::time::Duration>,
    max_retries: Option<u32>,
}

impl RetrieveRequestBuilder {
    fn new(client: crate::client::ProfoundClient, project_id: String, task_id: String, category_id: String) -> Self {
        Self {
            client,
            project_id,
            task_id,
            category_id,
            timeout: None,
            max_retries: None,
        }
    }

    /// Overrides the client's request deadline for this request only.
    /// It bounds each attempt up to response headers, then the buffered
    /// body read, so total wall time can exceed the value set here. A
    /// streamed body is never deadline-killed; see `ClientBuilder::timeout`.
    pub fn timeout(mut self, timeout: std::time::Duration) -> Self {
        self.timeout = Some(timeout);
        self
    }

    /// Overrides the client's maximum retry attempts for this request only.
    pub fn max_retries(mut self, max_retries: u32) -> Self {
        self.max_retries = Some(max_retries);
        self
    }

    /// Sends the request and returns the decoded response.
    ///
    /// # Errors
    ///
    /// - [`Error::Api`](crate::error::Error::Api) — the server answered with a
    ///   non-success status; the status, headers, request id, and decoded body are
    ///   preserved on the [`ApiError`](crate::error::ApiError).
    /// - [`Error::Transport`](crate::error::Error::Transport) — the request never
    ///   produced a response (connection failure, or the deadline elapsed).
    /// - [`Error::Config`](crate::error::Error::Config) — a parameter value could not
    ///   be rendered into the request.
    /// - [`Error::Serde`](crate::error::Error::Serde) — the response body did not
    ///   match the generated model.
    pub async fn send(self) -> Result<crate::models::ProjectTaskDetailResponse, crate::error::Error> {
        let path = format!(
            "/v1/projects/{}/tasks/{}",
            crate::http::encode_path_param(&self.project_id),
            crate::http::encode_path_param(&self.task_id)
        );
        let mut query: Vec<(String, String)> = Vec::new();
        let value = &self.category_id;
        query.push(("category_id".to_string(), crate::http::scalar_value(value)));
        let headers: Vec<(String, String)> = Vec::new();
        let body: Option<&serde_json::Value> = None;
        let overrides = crate::client::RequestOverrides {
            timeout: self.timeout,
            max_retries: self.max_retries,
        };
        self.client
            .send::<crate::models::ProjectTaskDetailResponse, _>(
                http::Method::GET,
                &path,
                &query,
                &headers,
                body,
                true,
                overrides,
            )
            .await
    }
}
/// Update Project Task
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct UpdateRequestBuilder {
    client: crate::client::ProfoundClient,
    project_id: String,
    task_id: String,
    category_id: String,
    body: crate::models::UpdateProjectTaskRequest,
    idempotency_key: Option<String>,
    timeout: Option<std::time::Duration>,
    max_retries: Option<u32>,
}

impl UpdateRequestBuilder {
    fn new(
        client: crate::client::ProfoundClient,
        project_id: String,
        task_id: String,
        category_id: String,
        body: crate::models::UpdateProjectTaskRequest,
    ) -> Self {
        Self {
            client,
            project_id,
            task_id,
            category_id,
            body,
            idempotency_key: None,
            timeout: None,
            max_retries: None,
        }
    }

    /// Sets an idempotency key so this request can be safely retried.
    pub fn idempotency_key(mut self, value: impl Into<String>) -> Self {
        self.idempotency_key = Some(value.into());
        self
    }

    /// Overrides the client's request deadline for this request only.
    /// It bounds each attempt up to response headers, then the buffered
    /// body read, so total wall time can exceed the value set here. A
    /// streamed body is never deadline-killed; see `ClientBuilder::timeout`.
    pub fn timeout(mut self, timeout: std::time::Duration) -> Self {
        self.timeout = Some(timeout);
        self
    }

    /// Overrides the client's maximum retry attempts for this request only.
    pub fn max_retries(mut self, max_retries: u32) -> Self {
        self.max_retries = Some(max_retries);
        self
    }

    /// Sends the request and returns the decoded response.
    ///
    /// # Errors
    ///
    /// - [`Error::Api`](crate::error::Error::Api) — the server answered with a
    ///   non-success status; the status, headers, request id, and decoded body are
    ///   preserved on the [`ApiError`](crate::error::ApiError).
    /// - [`Error::Transport`](crate::error::Error::Transport) — the request never
    ///   produced a response (connection failure, or the deadline elapsed).
    /// - [`Error::Config`](crate::error::Error::Config) — a parameter value could not
    ///   be rendered into the request.
    /// - [`Error::Serde`](crate::error::Error::Serde) — the response body did not
    ///   match the generated model.
    pub async fn send(self) -> Result<crate::models::ProjectTaskResponse, crate::error::Error> {
        let path = format!(
            "/v1/projects/{}/tasks/{}",
            crate::http::encode_path_param(&self.project_id),
            crate::http::encode_path_param(&self.task_id)
        );
        let mut query: Vec<(String, String)> = Vec::new();
        let value = &self.category_id;
        query.push(("category_id".to_string(), crate::http::scalar_value(value)));
        let mut headers: Vec<(String, String)> = Vec::new();
        if let Some(key) = self.idempotency_key {
            headers.push(("Idempotency-Key".to_string(), key));
        }
        let body = Some(&self.body);
        let overrides = crate::client::RequestOverrides {
            timeout: self.timeout,
            max_retries: self.max_retries,
        };
        self.client
            .send::<crate::models::ProjectTaskResponse, _>(
                http::Method::PATCH,
                &path,
                &query,
                &headers,
                body,
                true,
                overrides,
            )
            .await
    }
}
/// Delete Project Task
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct DeleteRequestBuilder {
    client: crate::client::ProfoundClient,
    project_id: String,
    task_id: String,
    category_id: String,
    idempotency_key: Option<String>,
    timeout: Option<std::time::Duration>,
    max_retries: Option<u32>,
}

impl DeleteRequestBuilder {
    fn new(client: crate::client::ProfoundClient, project_id: String, task_id: String, category_id: String) -> Self {
        Self {
            client,
            project_id,
            task_id,
            category_id,
            idempotency_key: None,
            timeout: None,
            max_retries: None,
        }
    }

    /// Sets an idempotency key so this request can be safely retried.
    pub fn idempotency_key(mut self, value: impl Into<String>) -> Self {
        self.idempotency_key = Some(value.into());
        self
    }

    /// Overrides the client's request deadline for this request only.
    /// It bounds each attempt up to response headers, then the buffered
    /// body read, so total wall time can exceed the value set here. A
    /// streamed body is never deadline-killed; see `ClientBuilder::timeout`.
    pub fn timeout(mut self, timeout: std::time::Duration) -> Self {
        self.timeout = Some(timeout);
        self
    }

    /// Overrides the client's maximum retry attempts for this request only.
    pub fn max_retries(mut self, max_retries: u32) -> Self {
        self.max_retries = Some(max_retries);
        self
    }

    /// Sends the request and returns the decoded response.
    ///
    /// # Errors
    ///
    /// - [`Error::Api`](crate::error::Error::Api) — the server answered with a
    ///   non-success status; the status, headers, request id, and decoded body are
    ///   preserved on the [`ApiError`](crate::error::ApiError).
    /// - [`Error::Transport`](crate::error::Error::Transport) — the request never
    ///   produced a response (connection failure, or the deadline elapsed).
    /// - [`Error::Config`](crate::error::Error::Config) — a parameter value could not
    ///   be rendered into the request.
    pub async fn send(self) -> Result<(), crate::error::Error> {
        let path = format!(
            "/v1/projects/{}/tasks/{}",
            crate::http::encode_path_param(&self.project_id),
            crate::http::encode_path_param(&self.task_id)
        );
        let mut query: Vec<(String, String)> = Vec::new();
        let value = &self.category_id;
        query.push(("category_id".to_string(), crate::http::scalar_value(value)));
        let mut headers: Vec<(String, String)> = vec![("Accept".to_string(), "*/*".to_string())];
        if let Some(key) = self.idempotency_key {
            headers.push(("Idempotency-Key".to_string(), key));
        }
        let body: Option<&serde_json::Value> = None;
        let overrides = crate::client::RequestOverrides {
            timeout: self.timeout,
            max_retries: self.max_retries,
        };
        self.client
            .send_empty(http::Method::DELETE, &path, &query, &headers, body, true, overrides)
            .await
    }
}
/// Update Project Task Status
#[must_use = "a request builder does nothing until `.send().await` is called"]
pub struct UpdateStatusRequestBuilder {
    client: crate::client::ProfoundClient,
    project_id: String,
    task_id: String,
    category_id: String,
    body: crate::models::UpdateProjectTaskStatusRequest,
    idempotency_key: Option<String>,
    timeout: Option<std::time::Duration>,
    max_retries: Option<u32>,
}

impl UpdateStatusRequestBuilder {
    fn new(
        client: crate::client::ProfoundClient,
        project_id: String,
        task_id: String,
        category_id: String,
        body: crate::models::UpdateProjectTaskStatusRequest,
    ) -> Self {
        Self {
            client,
            project_id,
            task_id,
            category_id,
            body,
            idempotency_key: None,
            timeout: None,
            max_retries: None,
        }
    }

    /// Sets an idempotency key so this request can be safely retried.
    pub fn idempotency_key(mut self, value: impl Into<String>) -> Self {
        self.idempotency_key = Some(value.into());
        self
    }

    /// Overrides the client's request deadline for this request only.
    /// It bounds each attempt up to response headers, then the buffered
    /// body read, so total wall time can exceed the value set here. A
    /// streamed body is never deadline-killed; see `ClientBuilder::timeout`.
    pub fn timeout(mut self, timeout: std::time::Duration) -> Self {
        self.timeout = Some(timeout);
        self
    }

    /// Overrides the client's maximum retry attempts for this request only.
    pub fn max_retries(mut self, max_retries: u32) -> Self {
        self.max_retries = Some(max_retries);
        self
    }

    /// Sends the request and returns the decoded response.
    ///
    /// # Errors
    ///
    /// - [`Error::Api`](crate::error::Error::Api) — the server answered with a
    ///   non-success status; the status, headers, request id, and decoded body are
    ///   preserved on the [`ApiError`](crate::error::ApiError).
    /// - [`Error::Transport`](crate::error::Error::Transport) — the request never
    ///   produced a response (connection failure, or the deadline elapsed).
    /// - [`Error::Config`](crate::error::Error::Config) — a parameter value could not
    ///   be rendered into the request.
    /// - [`Error::Serde`](crate::error::Error::Serde) — the response body did not
    ///   match the generated model.
    pub async fn send(self) -> Result<crate::models::ProjectTaskStatusResponse, crate::error::Error> {
        let path = format!(
            "/v1/projects/{}/tasks/{}/status",
            crate::http::encode_path_param(&self.project_id),
            crate::http::encode_path_param(&self.task_id)
        );
        let mut query: Vec<(String, String)> = Vec::new();
        let value = &self.category_id;
        query.push(("category_id".to_string(), crate::http::scalar_value(value)));
        let mut headers: Vec<(String, String)> = Vec::new();
        if let Some(key) = self.idempotency_key {
            headers.push(("Idempotency-Key".to_string(), key));
        }
        let body = Some(&self.body);
        let overrides = crate::client::RequestOverrides {
            timeout: self.timeout,
            max_retries: self.max_retries,
        };
        self.client
            .send::<crate::models::ProjectTaskStatusResponse, _>(
                http::Method::POST,
                &path,
                &query,
                &headers,
                body,
                true,
                overrides,
            )
            .await
    }
}
